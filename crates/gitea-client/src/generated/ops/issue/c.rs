// @generated by `cargo xtask codegen` from spec/gitea-1.27.3.json
// (sha256:52a2fbe7f08adfa7). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `issue` operations: `c`.
use gitea_core::Result;
use gitea_core::http::{Body, Part, Progress, Request, encode};
impl super::Issue<'_> {
    /// Check if a user can be assigned to an issue
    ///
    /// `GET /repos/{owner}/{repo}/issues/{index}/assignees/{assignee}` — needs the `read:issue` token scope.
    pub async fn check_assignee(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        assignee: &str,
    ) -> Result<()> {
        let req = Request::get(format!(
            "/repos/{}/{}/issues/{}/assignees/{}",
            encode::seg(owner),
            encode::seg(repo),
            index,
            encode::seg(assignee)
        ))
        .scope("read:issue");
        self.client.empty(req).await
    }
    /// Check if user is subscribed to an issue
    ///
    /// `GET /repos/{owner}/{repo}/issues/{index}/subscriptions/check` — needs the `read:issue` token scope.
    pub async fn check_subscription(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
    ) -> Result<gitea_model::WatchInfo> {
        let req = Request::get(format!(
            "/repos/{}/{}/issues/{}/subscriptions/check",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("read:issue");
        self.client.json(req).await
    }
    /// Remove all labels from an issue
    ///
    /// `DELETE /repos/{owner}/{repo}/issues/{index}/labels` — needs the `write:issue` token scope.
    pub async fn clear_labels(&self, owner: &str, repo: &str, index: i64) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/issues/{}/labels",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("write:issue");
        self.client.empty(req).await
    }
    /// Add a comment to an issue
    ///
    /// `POST /repos/{owner}/{repo}/issues/{index}/comments` — needs the `write:issue` token scope.
    pub async fn create_comment(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        body: &gitea_model::CreateIssueCommentOption,
    ) -> Result<gitea_model::Comment> {
        let req = Request::post(format!(
            "/repos/{}/{}/issues/{}/comments",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("write:issue")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Create an issue. If using deadline only the date will be taken into account, and time of day
    /// ignored.
    ///
    /// `POST /repos/{owner}/{repo}/issues` — needs the `write:issue` token scope.
    pub async fn create_issue(
        &self,
        owner: &str,
        repo: &str,
        body: &gitea_model::CreateIssueOption,
    ) -> Result<gitea_model::Issue> {
        let req =
            Request::post(format!("/repos/{}/{}/issues", encode::seg(owner), encode::seg(repo)))
                .scope("write:issue")
                .json_body(body)?;
        self.client.json(req).await
    }
    /// Create an issue attachment
    ///
    /// `POST /repos/{owner}/{repo}/issues/{index}/assets` — needs the `write:issue` token scope.
    pub async fn create_issue_attachment(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        attachment: Part,
        query: &crate::query::IssueCreateIssueAttachmentQuery,
        progress: Progress,
    ) -> Result<gitea_model::Attachment> {
        let parts: Vec<Part> = Vec::from([Part { name: "attachment".to_owned(), ..attachment }]);
        let req = query.apply(
            Request::post(format!(
                "/repos/{}/{}/issues/{}/assets",
                encode::seg(owner),
                encode::seg(repo),
                index
            ))
            .scope("write:issue")
            .body(Body::Multipart(parts))
            .progress(progress),
        );
        self.client.json(req).await
    }
    /// Block the issue given in the body by the issue in path
    ///
    /// `POST /repos/{owner}/{repo}/issues/{index}/blocks` — needs the `write:issue` token scope.
    pub async fn create_issue_blocking(
        &self,
        owner: &str,
        repo: &str,
        index: &str,
        body: &gitea_model::IssueMeta,
    ) -> Result<gitea_model::Issue> {
        let req = Request::post(format!(
            "/repos/{}/{}/issues/{}/blocks",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(index)
        ))
        .scope("write:issue")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Create a comment attachment
    ///
    /// `POST /repos/{owner}/{repo}/issues/comments/{id}/assets` — needs the `write:issue` token scope.
    pub async fn create_issue_comment_attachment(
        &self,
        owner: &str,
        repo: &str,
        id: i64,
        attachment: Part,
        query: &crate::query::IssueCreateIssueCommentAttachmentQuery,
        progress: Progress,
    ) -> Result<gitea_model::Attachment> {
        let parts: Vec<Part> = Vec::from([Part { name: "attachment".to_owned(), ..attachment }]);
        let req = query.apply(
            Request::post(format!(
                "/repos/{}/{}/issues/comments/{}/assets",
                encode::seg(owner),
                encode::seg(repo),
                id
            ))
            .scope("write:issue")
            .body(Body::Multipart(parts))
            .progress(progress),
        );
        self.client.json(req).await
    }
    /// Make the issue in the url depend on the issue in the form.
    ///
    /// `POST /repos/{owner}/{repo}/issues/{index}/dependencies` — needs the `write:issue` token scope.
    pub async fn create_issue_dependencies(
        &self,
        owner: &str,
        repo: &str,
        index: &str,
        body: &gitea_model::IssueMeta,
    ) -> Result<gitea_model::Issue> {
        let req = Request::post(format!(
            "/repos/{}/{}/issues/{}/dependencies",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(index)
        ))
        .scope("write:issue")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Create a label
    ///
    /// `POST /repos/{owner}/{repo}/labels` — needs the `write:issue` token scope.
    pub async fn create_label(
        &self,
        owner: &str,
        repo: &str,
        body: &gitea_model::CreateLabelOption,
    ) -> Result<gitea_model::Label> {
        let req =
            Request::post(format!("/repos/{}/{}/labels", encode::seg(owner), encode::seg(repo)))
                .scope("write:issue")
                .json_body(body)?;
        self.client.json(req).await
    }
    /// Create a milestone
    ///
    /// `POST /repos/{owner}/{repo}/milestones` — needs the `write:issue` token scope.
    pub async fn create_milestone(
        &self,
        owner: &str,
        repo: &str,
        body: &gitea_model::CreateMilestoneOption,
    ) -> Result<gitea_model::Milestone> {
        let req = Request::post(format!(
            "/repos/{}/{}/milestones",
            encode::seg(owner),
            encode::seg(repo)
        ))
        .scope("write:issue")
        .json_body(body)?;
        self.client.json(req).await
    }
}
