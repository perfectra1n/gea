// @generated by `cargo xtask codegen` from spec/gitea-1.27.3.json
// (sha256:52a2fbe7f08adfa7). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! Query-parameter structs for `admin`.
use gitea_core::http::Request;
/// Query parameters for `adminCronList`.
///
/// `GET /admin/cron`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct AdminCronListQuery {
    /// page size of results
    pub limit: Option<i32>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
}
impl AdminCronListQuery {
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `adminDeleteUser`.
///
/// `DELETE /admin/users/{username}`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct AdminDeleteUserQuery {
    /// purge the user from the system completely
    pub purge: Option<bool>,
}
impl AdminDeleteUserQuery {
    /// purge the user from the system completely
    pub fn with_purge(mut self, purge: bool) -> Self {
        self.purge = Some(purge);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.purge {
            out.push(("purge", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `getAdminRunners`.
///
/// `GET /admin/actions/runners`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct GetAdminRunnersQuery {
    /// filter by disabled status (true or false)
    pub disabled: Option<bool>,
}
impl GetAdminRunnersQuery {
    /// filter by disabled status (true or false)
    pub fn with_disabled(mut self, disabled: bool) -> Self {
        self.disabled = Some(disabled);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.disabled {
            out.push(("disabled", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `adminGetAllEmails`.
///
/// `GET /admin/emails`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct AdminGetAllEmailsQuery {
    /// page size of results
    pub limit: Option<i32>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
}
impl AdminGetAllEmailsQuery {
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `adminGetAllOrgs`.
///
/// `GET /admin/orgs`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct AdminGetAllOrgsQuery {
    /// page size of results
    pub limit: Option<i32>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
}
impl AdminGetAllOrgsQuery {
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `listAdminWorkflowJobs`.
///
/// `GET /admin/actions/jobs`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct ListAdminWorkflowJobsQuery {
    /// page size of results
    pub limit: Option<i32>,
    /// sort order, either "asc" (ascending) or "desc" (descending). Default is "asc"
    pub order: Option<String>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
    /// sort jobs by attribute. Supported values are "id". Default is "id"
    pub sort: Option<String>,
    /// workflow status (pending, queued, in_progress, failure, success, skipped)
    pub status: Option<String>,
}
impl ListAdminWorkflowJobsQuery {
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// sort order, either "asc" (ascending) or "desc" (descending). Default is "asc"
    pub fn with_order(mut self, order: &str) -> Self {
        self.order = Some(order.to_owned());
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// sort jobs by attribute. Supported values are "id". Default is "id"
    pub fn with_sort(mut self, sort: &str) -> Self {
        self.sort = Some(sort.to_owned());
        self
    }
    /// workflow status (pending, queued, in_progress, failure, success, skipped)
    pub fn with_status(mut self, status: &str) -> Self {
        self.status = Some(status.to_owned());
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.order {
            out.push(("order", v.clone()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        if let Some(v) = &self.sort {
            out.push(("sort", v.clone()));
        }
        if let Some(v) = &self.status {
            out.push(("status", v.clone()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `listAdminWorkflowRuns`.
///
/// `GET /admin/actions/runs`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct ListAdminWorkflowRunsQuery {
    /// triggered by user
    pub actor: Option<String>,
    /// workflow branch
    pub branch: Option<String>,
    /// workflow event name
    pub event: Option<String>,
    /// triggering sha of the workflow run
    pub head_sha: Option<String>,
    /// page size of results
    pub limit: Option<i32>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
    /// workflow status (pending, queued, in_progress, failure, success, skipped)
    pub status: Option<String>,
}
impl ListAdminWorkflowRunsQuery {
    /// triggered by user
    pub fn with_actor(mut self, actor: &str) -> Self {
        self.actor = Some(actor.to_owned());
        self
    }
    /// workflow branch
    pub fn with_branch(mut self, branch: &str) -> Self {
        self.branch = Some(branch.to_owned());
        self
    }
    /// workflow event name
    pub fn with_event(mut self, event: &str) -> Self {
        self.event = Some(event.to_owned());
        self
    }
    /// triggering sha of the workflow run
    pub fn with_head_sha(mut self, head_sha: &str) -> Self {
        self.head_sha = Some(head_sha.to_owned());
        self
    }
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// workflow status (pending, queued, in_progress, failure, success, skipped)
    pub fn with_status(mut self, status: &str) -> Self {
        self.status = Some(status.to_owned());
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.actor {
            out.push(("actor", v.clone()));
        }
        if let Some(v) = &self.branch {
            out.push(("branch", v.clone()));
        }
        if let Some(v) = &self.event {
            out.push(("event", v.clone()));
        }
        if let Some(v) = &self.head_sha {
            out.push(("head_sha", v.clone()));
        }
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        if let Some(v) = &self.status {
            out.push(("status", v.clone()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `adminListHooks`.
///
/// `GET /admin/hooks`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct AdminListHooksQuery {
    /// page size of results
    pub limit: Option<i32>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
    /// system, default or both kinds of webhooks
    ///
    /// The server defaults this to `system` when it is absent.
    /// Known values: `system`, `default`, `all`. Not enforced — a newer server may accept others.
    pub r#type: Option<String>,
}
impl AdminListHooksQuery {
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// system, default or both kinds of webhooks
    ///
    /// The server defaults this to `system` when it is absent.
    /// Known values: `system`, `default`, `all`. Not enforced — a newer server may accept others.
    pub fn with_type(mut self, r#type: &str) -> Self {
        self.r#type = Some(r#type.to_owned());
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        if let Some(v) = &self.r#type {
            out.push(("type", v.clone()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `adminSearchEmails`.
///
/// `GET /admin/emails/search`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct AdminSearchEmailsQuery {
    /// page size of results
    pub limit: Option<i32>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
    /// keyword
    pub q: Option<String>,
}
impl AdminSearchEmailsQuery {
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// keyword
    pub fn with_q(mut self, q: &str) -> Self {
        self.q = Some(q.to_owned());
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        if let Some(v) = &self.q {
            out.push(("q", v.clone()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `adminSearchUsers`.
///
/// `GET /admin/users`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct AdminSearchUsersQuery {
    /// filter 2FA enabled users
    pub is_2_fa_enabled: Option<bool>,
    /// filter active users
    pub is_active: Option<bool>,
    /// filter admin users
    pub is_admin: Option<bool>,
    /// filter login prohibited users
    pub is_prohibit_login: Option<bool>,
    /// filter restricted users
    pub is_restricted: Option<bool>,
    /// page size of results
    pub limit: Option<i32>,
    /// identifier of the user, provided by the external authenticator
    pub login_name: Option<String>,
    /// sort order, either "asc" (ascending) or "desc" (descending). Default is "asc", ignored if "sort"
    /// is not specified.
    pub order: Option<String>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
    /// search term (username, full name, email)
    pub q: Option<String>,
    /// sort users by attribute. Supported values are "name", "created", "updated" and "id". Default is
    /// "name"
    pub sort: Option<String>,
    /// ID of the user's login source to search for
    pub source_id: Option<i64>,
    /// visibility filter. Supported values are "public", "limited" and "private".
    pub visibility: Option<String>,
}
impl AdminSearchUsersQuery {
    /// filter 2FA enabled users
    pub fn with_is_2_fa_enabled(mut self, is_2_fa_enabled: bool) -> Self {
        self.is_2_fa_enabled = Some(is_2_fa_enabled);
        self
    }
    /// filter active users
    pub fn with_is_active(mut self, is_active: bool) -> Self {
        self.is_active = Some(is_active);
        self
    }
    /// filter admin users
    pub fn with_is_admin(mut self, is_admin: bool) -> Self {
        self.is_admin = Some(is_admin);
        self
    }
    /// filter login prohibited users
    pub fn with_is_prohibit_login(mut self, is_prohibit_login: bool) -> Self {
        self.is_prohibit_login = Some(is_prohibit_login);
        self
    }
    /// filter restricted users
    pub fn with_is_restricted(mut self, is_restricted: bool) -> Self {
        self.is_restricted = Some(is_restricted);
        self
    }
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// identifier of the user, provided by the external authenticator
    pub fn with_login_name(mut self, login_name: &str) -> Self {
        self.login_name = Some(login_name.to_owned());
        self
    }
    /// sort order, either "asc" (ascending) or "desc" (descending). Default is "asc", ignored if "sort"
    /// is not specified.
    pub fn with_order(mut self, order: &str) -> Self {
        self.order = Some(order.to_owned());
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// search term (username, full name, email)
    pub fn with_q(mut self, q: &str) -> Self {
        self.q = Some(q.to_owned());
        self
    }
    /// sort users by attribute. Supported values are "name", "created", "updated" and "id". Default is
    /// "name"
    pub fn with_sort(mut self, sort: &str) -> Self {
        self.sort = Some(sort.to_owned());
        self
    }
    /// ID of the user's login source to search for
    pub fn with_source_id(mut self, source_id: i64) -> Self {
        self.source_id = Some(source_id);
        self
    }
    /// visibility filter. Supported values are "public", "limited" and "private".
    pub fn with_visibility(mut self, visibility: &str) -> Self {
        self.visibility = Some(visibility.to_owned());
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.is_2_fa_enabled {
            out.push(("is_2fa_enabled", v.to_string()));
        }
        if let Some(v) = &self.is_active {
            out.push(("is_active", v.to_string()));
        }
        if let Some(v) = &self.is_admin {
            out.push(("is_admin", v.to_string()));
        }
        if let Some(v) = &self.is_prohibit_login {
            out.push(("is_prohibit_login", v.to_string()));
        }
        if let Some(v) = &self.is_restricted {
            out.push(("is_restricted", v.to_string()));
        }
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.login_name {
            out.push(("login_name", v.clone()));
        }
        if let Some(v) = &self.order {
            out.push(("order", v.clone()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        if let Some(v) = &self.q {
            out.push(("q", v.clone()));
        }
        if let Some(v) = &self.sort {
            out.push(("sort", v.clone()));
        }
        if let Some(v) = &self.source_id {
            out.push(("source_id", v.to_string()));
        }
        if let Some(v) = &self.visibility {
            out.push(("visibility", v.clone()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `adminUnadoptedList`.
///
/// `GET /admin/unadopted`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct AdminUnadoptedListQuery {
    /// page size of results
    pub limit: Option<i32>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
    /// pattern of repositories to search for
    pub pattern: Option<String>,
}
impl AdminUnadoptedListQuery {
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// pattern of repositories to search for
    pub fn with_pattern(mut self, pattern: &str) -> Self {
        self.pattern = Some(pattern.to_owned());
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        if let Some(v) = &self.pattern {
            out.push(("pattern", v.clone()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
