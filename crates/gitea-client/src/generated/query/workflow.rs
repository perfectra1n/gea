// @generated by `cargo xtask codegen` from spec/gitea-1.27.3.json
// (sha256:52a2fbe7f08adfa7). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! Query-parameter structs for `workflow`.
use gitea_core::http::Request;
/// Query parameters for `ActionsDispatchWorkflow`.
///
/// `POST /repos/{owner}/{repo}/actions/workflows/{workflow_id}/dispatches`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct ActionsDispatchWorkflowQuery {
    /// Whether the response should include the workflow run ID and URLs.
    pub return_run_details: Option<bool>,
    /// For a scoped workflow, the ID of the source repository providing it; omit or 0 for a repo-level
    /// workflow.
    pub scoped_workflow_source_repo_id: Option<i64>,
}
impl ActionsDispatchWorkflowQuery {
    /// Whether the response should include the workflow run ID and URLs.
    pub fn with_return_run_details(mut self, return_run_details: bool) -> Self {
        self.return_run_details = Some(return_run_details);
        self
    }
    /// For a scoped workflow, the ID of the source repository providing it; omit or 0 for a repo-level
    /// workflow.
    pub fn with_scoped_workflow_source_repo_id(
        mut self,
        scoped_workflow_source_repo_id: i64,
    ) -> Self {
        self.scoped_workflow_source_repo_id = Some(scoped_workflow_source_repo_id);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.return_run_details {
            out.push(("return_run_details", v.to_string()));
        }
        if let Some(v) = &self.scoped_workflow_source_repo_id {
            out.push(("scoped_workflow_source_repo_id", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `ActionsListWorkflowRuns`.
///
/// `GET /repos/{owner}/{repo}/actions/workflows/{workflow_id}/runs`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct ActionsListWorkflowRunsQuery {
    /// triggered by user
    pub actor: Option<String>,
    /// workflow branch
    pub branch: Option<String>,
    /// workflow event name
    pub event: Option<String>,
    /// if true, the `pull_requests` field on each returned run is emptied
    pub exclude_pull_requests: Option<bool>,
    /// triggering sha of the workflow run
    pub head_sha: Option<String>,
    /// page size of results
    pub limit: Option<i32>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
    /// For a scoped workflow, the ID of the source repository providing it; omit or 0 for a repo-level
    /// workflow.
    pub scoped_workflow_source_repo_id: Option<i64>,
    /// workflow status (pending, queued, in_progress, failure, success, skipped)
    pub status: Option<String>,
}
impl ActionsListWorkflowRunsQuery {
    /// triggered by user
    pub fn with_actor(mut self, actor: &str) -> Self {
        self.actor = Some(actor.to_owned());
        self
    }
    /// workflow branch
    pub fn with_branch(mut self, branch: &str) -> Self {
        self.branch = Some(branch.to_owned());
        self
    }
    /// workflow event name
    pub fn with_event(mut self, event: &str) -> Self {
        self.event = Some(event.to_owned());
        self
    }
    /// if true, the `pull_requests` field on each returned run is emptied
    pub fn with_exclude_pull_requests(mut self, exclude_pull_requests: bool) -> Self {
        self.exclude_pull_requests = Some(exclude_pull_requests);
        self
    }
    /// triggering sha of the workflow run
    pub fn with_head_sha(mut self, head_sha: &str) -> Self {
        self.head_sha = Some(head_sha.to_owned());
        self
    }
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// For a scoped workflow, the ID of the source repository providing it; omit or 0 for a repo-level
    /// workflow.
    pub fn with_scoped_workflow_source_repo_id(
        mut self,
        scoped_workflow_source_repo_id: i64,
    ) -> Self {
        self.scoped_workflow_source_repo_id = Some(scoped_workflow_source_repo_id);
        self
    }
    /// workflow status (pending, queued, in_progress, failure, success, skipped)
    pub fn with_status(mut self, status: &str) -> Self {
        self.status = Some(status.to_owned());
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.actor {
            out.push(("actor", v.clone()));
        }
        if let Some(v) = &self.branch {
            out.push(("branch", v.clone()));
        }
        if let Some(v) = &self.event {
            out.push(("event", v.clone()));
        }
        if let Some(v) = &self.exclude_pull_requests {
            out.push(("exclude_pull_requests", v.to_string()));
        }
        if let Some(v) = &self.head_sha {
            out.push(("head_sha", v.clone()));
        }
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        if let Some(v) = &self.scoped_workflow_source_repo_id {
            out.push(("scoped_workflow_source_repo_id", v.to_string()));
        }
        if let Some(v) = &self.status {
            out.push(("status", v.clone()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
