// @generated by `cargo xtask codegen` from spec/gitea-1.27.3.json
// (sha256:52a2fbe7f08adfa7). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! Query-parameter structs for `notify`.
use gitea_core::http::Request;
/// Query parameters for `notifyGetList`.
///
/// `GET /notifications`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct NotifyGetListQuery {
    /// If true, show notifications marked as read. Default value is false
    pub all: Option<bool>,
    /// Only show notifications updated before the given time. This is a timestamp in RFC 3339 format
    pub before: Option<gitea_core::types::Timestamp>,
    /// page size of results
    pub limit: Option<i32>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
    /// Only show notifications updated after the given time. This is a timestamp in RFC 3339 format
    pub since: Option<gitea_core::types::Timestamp>,
    /// Show notifications with the provided status types. Options are: unread, read and/or pinned.
    /// Defaults to unread & pinned.
    pub status_types: Vec<String>,
    /// filter notifications by subject type
    ///
    /// Known values: `issue`, `pull`, `commit`, `repository`. Not enforced — a newer server may accept others.
    pub subject_type: Vec<String>,
}
impl NotifyGetListQuery {
    /// If true, show notifications marked as read. Default value is false
    pub fn with_all(mut self, all: bool) -> Self {
        self.all = Some(all);
        self
    }
    /// Only show notifications updated before the given time. This is a timestamp in RFC 3339 format
    pub fn with_before(mut self, before: gitea_core::types::Timestamp) -> Self {
        self.before = Some(before);
        self
    }
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// Only show notifications updated after the given time. This is a timestamp in RFC 3339 format
    pub fn with_since(mut self, since: gitea_core::types::Timestamp) -> Self {
        self.since = Some(since);
        self
    }
    /// Show notifications with the provided status types. Options are: unread, read and/or pinned.
    /// Defaults to unread & pinned.
    pub fn with_status_types(mut self, status_types: Vec<String>) -> Self {
        self.status_types = status_types;
        self
    }
    /// filter notifications by subject type
    ///
    /// Known values: `issue`, `pull`, `commit`, `repository`. Not enforced — a newer server may accept others.
    pub fn with_subject_type(mut self, subject_type: Vec<String>) -> Self {
        self.subject_type = subject_type;
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.all {
            out.push(("all", v.to_string()));
        }
        if let Some(v) = &self.before {
            out.push(("before", v.to_string()));
        }
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        if let Some(v) = &self.since {
            out.push(("since", v.to_string()));
        }
        for v in &self.status_types {
            out.push(("status-types", v.clone()));
        }
        for v in &self.subject_type {
            out.push(("subject-type", v.clone()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `notifyGetRepoList`.
///
/// `GET /repos/{owner}/{repo}/notifications`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct NotifyGetRepoListQuery {
    /// If true, show notifications marked as read. Default value is false
    pub all: Option<bool>,
    /// Only show notifications updated before the given time. This is a timestamp in RFC 3339 format
    pub before: Option<gitea_core::types::Timestamp>,
    /// page size of results
    pub limit: Option<i32>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
    /// Only show notifications updated after the given time. This is a timestamp in RFC 3339 format
    pub since: Option<gitea_core::types::Timestamp>,
    /// Show notifications with the provided status types. Options are: unread, read and/or pinned.
    /// Defaults to unread & pinned
    pub status_types: Vec<String>,
    /// filter notifications by subject type
    ///
    /// Known values: `issue`, `pull`, `commit`, `repository`. Not enforced — a newer server may accept others.
    pub subject_type: Vec<String>,
}
impl NotifyGetRepoListQuery {
    /// If true, show notifications marked as read. Default value is false
    pub fn with_all(mut self, all: bool) -> Self {
        self.all = Some(all);
        self
    }
    /// Only show notifications updated before the given time. This is a timestamp in RFC 3339 format
    pub fn with_before(mut self, before: gitea_core::types::Timestamp) -> Self {
        self.before = Some(before);
        self
    }
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// Only show notifications updated after the given time. This is a timestamp in RFC 3339 format
    pub fn with_since(mut self, since: gitea_core::types::Timestamp) -> Self {
        self.since = Some(since);
        self
    }
    /// Show notifications with the provided status types. Options are: unread, read and/or pinned.
    /// Defaults to unread & pinned
    pub fn with_status_types(mut self, status_types: Vec<String>) -> Self {
        self.status_types = status_types;
        self
    }
    /// filter notifications by subject type
    ///
    /// Known values: `issue`, `pull`, `commit`, `repository`. Not enforced — a newer server may accept others.
    pub fn with_subject_type(mut self, subject_type: Vec<String>) -> Self {
        self.subject_type = subject_type;
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.all {
            out.push(("all", v.to_string()));
        }
        if let Some(v) = &self.before {
            out.push(("before", v.to_string()));
        }
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        if let Some(v) = &self.since {
            out.push(("since", v.to_string()));
        }
        for v in &self.status_types {
            out.push(("status-types", v.clone()));
        }
        for v in &self.subject_type {
            out.push(("subject-type", v.clone()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `notifyReadList`.
///
/// `PUT /notifications`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct NotifyReadListQuery {
    /// If true, mark all notifications on this repo. Default value is false
    pub all: Option<String>,
    /// Describes the last point that notifications were checked. Anything updated since this time will
    /// not be updated.
    pub last_read_at: Option<gitea_core::types::Timestamp>,
    /// Mark notifications with the provided status types. Options are: unread, read and/or pinned.
    /// Defaults to unread.
    pub status_types: Vec<String>,
    /// Status to mark notifications as, Defaults to read.
    pub to_status: Option<String>,
}
impl NotifyReadListQuery {
    /// If true, mark all notifications on this repo. Default value is false
    pub fn with_all(mut self, all: &str) -> Self {
        self.all = Some(all.to_owned());
        self
    }
    /// Describes the last point that notifications were checked. Anything updated since this time will
    /// not be updated.
    pub fn with_last_read_at(mut self, last_read_at: gitea_core::types::Timestamp) -> Self {
        self.last_read_at = Some(last_read_at);
        self
    }
    /// Mark notifications with the provided status types. Options are: unread, read and/or pinned.
    /// Defaults to unread.
    pub fn with_status_types(mut self, status_types: Vec<String>) -> Self {
        self.status_types = status_types;
        self
    }
    /// Status to mark notifications as, Defaults to read.
    pub fn with_to_status(mut self, to_status: &str) -> Self {
        self.to_status = Some(to_status.to_owned());
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.all {
            out.push(("all", v.clone()));
        }
        if let Some(v) = &self.last_read_at {
            out.push(("last_read_at", v.to_string()));
        }
        for v in &self.status_types {
            out.push(("status-types", v.clone()));
        }
        if let Some(v) = &self.to_status {
            out.push(("to-status", v.clone()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `notifyReadRepoList`.
///
/// `PUT /repos/{owner}/{repo}/notifications`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct NotifyReadRepoListQuery {
    /// If true, mark all notifications on this repo. Default value is false
    pub all: Option<String>,
    /// Describes the last point that notifications were checked. Anything updated since this time will
    /// not be updated.
    pub last_read_at: Option<gitea_core::types::Timestamp>,
    /// Mark notifications with the provided status types. Options are: unread, read and/or pinned.
    /// Defaults to unread.
    pub status_types: Vec<String>,
    /// Status to mark notifications as. Defaults to read.
    pub to_status: Option<String>,
}
impl NotifyReadRepoListQuery {
    /// If true, mark all notifications on this repo. Default value is false
    pub fn with_all(mut self, all: &str) -> Self {
        self.all = Some(all.to_owned());
        self
    }
    /// Describes the last point that notifications were checked. Anything updated since this time will
    /// not be updated.
    pub fn with_last_read_at(mut self, last_read_at: gitea_core::types::Timestamp) -> Self {
        self.last_read_at = Some(last_read_at);
        self
    }
    /// Mark notifications with the provided status types. Options are: unread, read and/or pinned.
    /// Defaults to unread.
    pub fn with_status_types(mut self, status_types: Vec<String>) -> Self {
        self.status_types = status_types;
        self
    }
    /// Status to mark notifications as. Defaults to read.
    pub fn with_to_status(mut self, to_status: &str) -> Self {
        self.to_status = Some(to_status.to_owned());
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.all {
            out.push(("all", v.clone()));
        }
        if let Some(v) = &self.last_read_at {
            out.push(("last_read_at", v.to_string()));
        }
        for v in &self.status_types {
            out.push(("status-types", v.clone()));
        }
        if let Some(v) = &self.to_status {
            out.push(("to-status", v.clone()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
/// Query parameters for `notifyReadThread`.
///
/// `PATCH /notifications/threads/{id}`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct NotifyReadThreadQuery {
    /// Status to mark notifications as
    ///
    /// The server defaults this to `read` when it is absent.
    pub to_status: Option<String>,
}
impl NotifyReadThreadQuery {
    /// Status to mark notifications as
    ///
    /// The server defaults this to `read` when it is absent.
    pub fn with_to_status(mut self, to_status: &str) -> Self {
        self.to_status = Some(to_status.to_owned());
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.to_status {
            out.push(("to-status", v.clone()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
