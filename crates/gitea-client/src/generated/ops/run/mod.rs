// @generated by `cargo xtask codegen` from spec/gitea-1.27.3.json
// (sha256:52a2fbe7f08adfa7). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `run` — Actions runs.
use gitea_core::Result;
use gitea_core::http::{Client, Request, encode};
/// Actions runs.
///
/// Obtain one from `Api::run`, or wrap a bare `Client` with `Run::new`.
#[derive(Debug, Clone, Copy)]
pub struct Run<'a> {
    pub(crate) client: &'a Client,
}
impl<'a> Run<'a> {
    /// Borrow a client as this operation group. Free — no allocation, no clone.
    pub fn new(client: &'a Client) -> Self {
        Self { client }
    }
    /// Lists all artifacts for a repository run
    ///
    /// `GET /repos/{owner}/{repo}/actions/runs/{run}/artifacts` — needs the `read:repository` token scope.
    pub async fn artifacts(
        &self,
        owner: &str,
        repo: &str,
        run: i32,
        query: &crate::query::GetArtifactsOfRunQuery,
    ) -> Result<gitea_model::ActionArtifactsResponse> {
        let req = query.apply(
            Request::get(format!(
                "/repos/{}/{}/actions/runs/{}/artifacts",
                encode::seg(owner),
                encode::seg(repo),
                run
            ))
            .scope("read:repository"),
        );
        self.client.json(req).await
    }
    /// Gets a specific workflow run attempt
    ///
    /// `GET /repos/{owner}/{repo}/actions/runs/{run}/attempts/{attempt}` — needs the `read:repository` token scope.
    pub async fn attempt(
        &self,
        owner: &str,
        repo: &str,
        run: i32,
        attempt: i32,
    ) -> Result<gitea_model::ActionWorkflowRun> {
        let req = Request::get(format!(
            "/repos/{}/{}/actions/runs/{}/attempts/{}",
            encode::seg(owner),
            encode::seg(repo),
            run,
            attempt
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// Lists all jobs for a workflow run attempt
    ///
    /// `GET /repos/{owner}/{repo}/actions/runs/{run}/attempts/{attempt}/jobs` — needs the `read:repository` token scope.
    pub async fn attempt_jobs(
        &self,
        owner: &str,
        repo: &str,
        run: i32,
        attempt: i32,
        query: &crate::query::ListWorkflowRunAttemptJobsQuery,
    ) -> Result<gitea_model::ActionWorkflowJobsResponse> {
        let req = query.apply(
            Request::get(format!(
                "/repos/{}/{}/actions/runs/{}/attempts/{}/jobs",
                encode::seg(owner),
                encode::seg(repo),
                run,
                attempt
            ))
            .scope("read:repository"),
        );
        self.client.json(req).await
    }
    /// Delete a workflow run
    ///
    /// `DELETE /repos/{owner}/{repo}/actions/runs/{run}` — needs the `write:repository` token scope.
    pub async fn delete(&self, owner: &str, repo: &str, run: i32) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/actions/runs/{}",
            encode::seg(owner),
            encode::seg(repo),
            run
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Lists all jobs for a workflow run
    ///
    /// `GET /repos/{owner}/{repo}/actions/runs/{run}/jobs` — needs the `read:repository` token scope.
    pub async fn jobs(
        &self,
        owner: &str,
        repo: &str,
        run: i32,
        query: &crate::query::ListWorkflowRunJobsQuery,
    ) -> Result<gitea_model::ActionWorkflowJobsResponse> {
        let req = query.apply(
            Request::get(format!(
                "/repos/{}/{}/actions/runs/{}/jobs",
                encode::seg(owner),
                encode::seg(repo),
                run
            ))
            .scope("read:repository"),
        );
        self.client.json(req).await
    }
    /// Lists all runs for a repository run
    ///
    /// `GET /repos/{owner}/{repo}/actions/runs` — needs the `read:repository` token scope.
    pub async fn list(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::GetWorkflowRunsQuery,
    ) -> Result<gitea_model::ActionWorkflowRunsResponse> {
        let req = query.apply(
            Request::get(format!(
                "/repos/{}/{}/actions/runs",
                encode::seg(owner),
                encode::seg(repo)
            ))
            .scope("read:repository"),
        );
        self.client.json(req).await
    }
    /// Reruns an entire workflow run
    ///
    /// `POST /repos/{owner}/{repo}/actions/runs/{run}/rerun` — needs the `write:repository` token scope.
    pub async fn rerun(
        &self,
        owner: &str,
        repo: &str,
        run: i32,
    ) -> Result<gitea_model::ActionWorkflowRun> {
        let req = Request::post(format!(
            "/repos/{}/{}/actions/runs/{}/rerun",
            encode::seg(owner),
            encode::seg(repo),
            run
        ))
        .scope("write:repository");
        self.client.json(req).await
    }
    /// Reruns all failed jobs in a workflow run
    ///
    /// `POST /repos/{owner}/{repo}/actions/runs/{run}/rerun-failed-jobs` — needs the `write:repository` token scope.
    pub async fn rerun_failed(&self, owner: &str, repo: &str, run: i32) -> Result<()> {
        let req = Request::post(format!(
            "/repos/{}/{}/actions/runs/{}/rerun-failed-jobs",
            encode::seg(owner),
            encode::seg(repo),
            run
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Reruns a specific workflow job in a run
    ///
    /// `POST /repos/{owner}/{repo}/actions/runs/{run}/jobs/{job_id}/rerun` — needs the `write:repository` token scope.
    pub async fn rerun_job(
        &self,
        owner: &str,
        repo: &str,
        run: i32,
        job_id: i32,
    ) -> Result<gitea_model::ActionWorkflowJob> {
        let req = Request::post(format!(
            "/repos/{}/{}/actions/runs/{}/jobs/{}/rerun",
            encode::seg(owner),
            encode::seg(repo),
            run,
            job_id
        ))
        .scope("write:repository");
        self.client.json(req).await
    }
    /// Gets a specific workflow run
    ///
    /// `GET /repos/{owner}/{repo}/actions/runs/{run}` — needs the `read:repository` token scope.
    pub async fn view(
        &self,
        owner: &str,
        repo: &str,
        run: i32,
    ) -> Result<gitea_model::ActionWorkflowRun> {
        let req = Request::get(format!(
            "/repos/{}/{}/actions/runs/{}",
            encode::seg(owner),
            encode::seg(repo),
            run
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
}
