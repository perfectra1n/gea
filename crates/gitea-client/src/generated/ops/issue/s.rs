// @generated by `cargo xtask codegen` from spec/gitea-1.27.3.json
// (sha256:52a2fbe7f08adfa7). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `issue` operations: `s`.
use gitea_core::Result;
use gitea_core::http::{ItemStream, PageInfo, Paging, Request, encode};
impl super::Issue<'_> {
    /// Search for issues across the repositories that the user has access to
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Gitea silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/issues/search` — needs the `read:issue` token scope.
    pub fn search_issues(
        &self,
        query: &crate::query::IssueSearchIssuesQuery,
    ) -> ItemStream<gitea_model::Issue> {
        let req = crate::stream_request(
            query.apply(Request::get("/repos/issues/search").scope("read:issue")),
            "page",
        );
        self.client.items(req)
    }
    /// Search for issues across the repositories that the user has access to
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/issues/search` — needs the `read:issue` token scope.
    pub async fn search_issues_page(
        &self,
        query: &crate::query::IssueSearchIssuesQuery,
        paging: Paging,
    ) -> Result<(Vec<gitea_model::Issue>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/repos/issues/search").scope("read:issue")),
            paging,
        );
        self.client.page(req).await
    }
    /// Start stopwatch on an issue.
    ///
    /// `POST /repos/{owner}/{repo}/issues/{index}/stopwatch/start` — needs the `write:issue` token scope.
    pub async fn start_stop_watch(&self, owner: &str, repo: &str, index: i64) -> Result<()> {
        let req = Request::post(format!(
            "/repos/{}/{}/issues/{}/stopwatch/start",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("write:issue");
        self.client.empty(req).await
    }
    /// Stop an issue's existing stopwatch.
    ///
    /// `POST /repos/{owner}/{repo}/issues/{index}/stopwatch/stop` — needs the `write:issue` token scope.
    pub async fn stop_stop_watch(&self, owner: &str, repo: &str, index: i64) -> Result<()> {
        let req = Request::post(format!(
            "/repos/{}/{}/issues/{}/stopwatch/stop",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("write:issue");
        self.client.empty(req).await
    }
    /// Get users who subscribed on an issue.
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Gitea silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/issues/{index}/subscriptions` — needs the `read:issue` token scope.
    pub fn subscriptions(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        query: &crate::query::IssueSubscriptionsQuery,
    ) -> ItemStream<gitea_model::User> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/issues/{}/subscriptions",
                    encode::seg(owner),
                    encode::seg(repo),
                    index
                ))
                .scope("read:issue"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// Get users who subscribed on an issue.
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/issues/{index}/subscriptions` — needs the `read:issue` token scope.
    pub async fn subscriptions_page(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        query: &crate::query::IssueSubscriptionsQuery,
        paging: Paging,
    ) -> Result<(Vec<gitea_model::User>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/issues/{}/subscriptions",
                    encode::seg(owner),
                    encode::seg(repo),
                    index
                ))
                .scope("read:issue"),
            ),
            paging,
        );
        self.client.page(req).await
    }
}
