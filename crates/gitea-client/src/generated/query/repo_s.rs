// @generated by `cargo xtask codegen` from spec/gitea-1.27.3.json
// (sha256:52a2fbe7f08adfa7). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! Query-parameter structs for `repo_s`.
use gitea_core::http::Request;
/// Query parameters for `repoSearch`.
///
/// `GET /repos/search`.
///
/// Every field is optional and `Default` sends nothing at all, so adding a
/// parameter upstream cannot change what an existing call sends.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct RepoSearchQuery {
    /// show only archived, non-archived or all repositories (defaults to all)
    pub archived: Option<bool>,
    /// if `uid` is given, search only for repos that the user owns
    pub exclusive: Option<bool>,
    /// include search of keyword within repository description
    pub include_desc: Option<bool>,
    /// show only pubic, private or all repositories (defaults to all)
    pub is_private: Option<bool>,
    /// page size of results
    pub limit: Option<i32>,
    /// type of repository to search for. Supported values are "fork", "source", "mirror" and
    /// "collaborative"
    pub mode: Option<String>,
    /// sort order, either "asc" (ascending) or "desc" (descending). Default is "asc", ignored if "sort"
    /// is not specified.
    pub order: Option<String>,
    /// page number of results to return (1-based)
    pub page: Option<i32>,
    /// repo owner to prioritize in the results
    pub priority_owner_id: Option<i64>,
    /// include private repositories this user has access to (defaults to true)
    pub private: Option<bool>,
    /// keyword
    pub q: Option<String>,
    /// sort repos by attribute. Supported values are "alpha", "created", "updated", "size", "git_size",
    /// "lfs_size", "stars", "forks" and "id". Default is "alpha"
    pub sort: Option<String>,
    /// search only for repos that the user with the given id has starred
    pub starred_by: Option<i64>,
    /// search only for repos that belong to the given team id
    pub team_id: Option<i64>,
    /// include template repositories this user has access to (defaults to true)
    pub template: Option<bool>,
    /// Limit search to repositories with keyword as topic
    pub topic: Option<bool>,
    /// search only for repos that the user with the given id owns or contributes to
    pub uid: Option<i64>,
}
impl RepoSearchQuery {
    /// show only archived, non-archived or all repositories (defaults to all)
    pub fn with_archived(mut self, archived: bool) -> Self {
        self.archived = Some(archived);
        self
    }
    /// if `uid` is given, search only for repos that the user owns
    pub fn with_exclusive(mut self, exclusive: bool) -> Self {
        self.exclusive = Some(exclusive);
        self
    }
    /// include search of keyword within repository description
    pub fn with_include_desc(mut self, include_desc: bool) -> Self {
        self.include_desc = Some(include_desc);
        self
    }
    /// show only pubic, private or all repositories (defaults to all)
    pub fn with_is_private(mut self, is_private: bool) -> Self {
        self.is_private = Some(is_private);
        self
    }
    /// page size of results
    pub fn with_limit(mut self, limit: i32) -> Self {
        self.limit = Some(limit);
        self
    }
    /// type of repository to search for. Supported values are "fork", "source", "mirror" and
    /// "collaborative"
    pub fn with_mode(mut self, mode: &str) -> Self {
        self.mode = Some(mode.to_owned());
        self
    }
    /// sort order, either "asc" (ascending) or "desc" (descending). Default is "asc", ignored if "sort"
    /// is not specified.
    pub fn with_order(mut self, order: &str) -> Self {
        self.order = Some(order.to_owned());
        self
    }
    /// page number of results to return (1-based)
    pub fn with_page(mut self, page: i32) -> Self {
        self.page = Some(page);
        self
    }
    /// repo owner to prioritize in the results
    pub fn with_priority_owner_id(mut self, priority_owner_id: i64) -> Self {
        self.priority_owner_id = Some(priority_owner_id);
        self
    }
    /// include private repositories this user has access to (defaults to true)
    pub fn with_private(mut self, private: bool) -> Self {
        self.private = Some(private);
        self
    }
    /// keyword
    pub fn with_q(mut self, q: &str) -> Self {
        self.q = Some(q.to_owned());
        self
    }
    /// sort repos by attribute. Supported values are "alpha", "created", "updated", "size", "git_size",
    /// "lfs_size", "stars", "forks" and "id". Default is "alpha"
    pub fn with_sort(mut self, sort: &str) -> Self {
        self.sort = Some(sort.to_owned());
        self
    }
    /// search only for repos that the user with the given id has starred
    pub fn with_starred_by(mut self, starred_by: i64) -> Self {
        self.starred_by = Some(starred_by);
        self
    }
    /// search only for repos that belong to the given team id
    pub fn with_team_id(mut self, team_id: i64) -> Self {
        self.team_id = Some(team_id);
        self
    }
    /// include template repositories this user has access to (defaults to true)
    pub fn with_template(mut self, template: bool) -> Self {
        self.template = Some(template);
        self
    }
    /// Limit search to repositories with keyword as topic
    pub fn with_topic(mut self, topic: bool) -> Self {
        self.topic = Some(topic);
        self
    }
    /// search only for repos that the user with the given id owns or contributes to
    pub fn with_uid(mut self, uid: i64) -> Self {
        self.uid = Some(uid);
        self
    }
    /// The wire pairs, in a stable order: sorted by parameter name, with a
    /// repeated key per element of a list. Stable because a request URL that
    /// moves between runs makes every snapshot test flap.
    pub fn to_pairs(&self) -> Vec<(&'static str, String)> {
        let mut out: Vec<(&'static str, String)> = Vec::new();
        if let Some(v) = &self.archived {
            out.push(("archived", v.to_string()));
        }
        if let Some(v) = &self.exclusive {
            out.push(("exclusive", v.to_string()));
        }
        if let Some(v) = &self.include_desc {
            out.push(("includeDesc", v.to_string()));
        }
        if let Some(v) = &self.is_private {
            out.push(("is_private", v.to_string()));
        }
        if let Some(v) = &self.limit {
            out.push(("limit", v.to_string()));
        }
        if let Some(v) = &self.mode {
            out.push(("mode", v.clone()));
        }
        if let Some(v) = &self.order {
            out.push(("order", v.clone()));
        }
        if let Some(v) = &self.page {
            out.push(("page", v.to_string()));
        }
        if let Some(v) = &self.priority_owner_id {
            out.push(("priority_owner_id", v.to_string()));
        }
        if let Some(v) = &self.private {
            out.push(("private", v.to_string()));
        }
        if let Some(v) = &self.q {
            out.push(("q", v.clone()));
        }
        if let Some(v) = &self.sort {
            out.push(("sort", v.clone()));
        }
        if let Some(v) = &self.starred_by {
            out.push(("starredBy", v.to_string()));
        }
        if let Some(v) = &self.team_id {
            out.push(("team_id", v.to_string()));
        }
        if let Some(v) = &self.template {
            out.push(("template", v.to_string()));
        }
        if let Some(v) = &self.topic {
            out.push(("topic", v.to_string()));
        }
        if let Some(v) = &self.uid {
            out.push(("uid", v.to_string()));
        }
        out
    }
    /// Append every set parameter to `req`, skipping the unset ones.
    pub fn apply(&self, mut req: Request) -> Request {
        for (k, v) in self.to_pairs() {
            req = req.query(k, v);
        }
        req
    }
}
