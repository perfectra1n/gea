// @generated by `cargo xtask codegen` from spec/gitea-1.27.3.json
// (sha256:52a2fbe7f08adfa7). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `job` — Actions jobs.
use gitea_core::Result;
use gitea_core::http::{Accept, ByteStream, Client, Mime, Request, encode};
/// Actions jobs.
///
/// Obtain one from `Api::job`, or wrap a bare `Client` with `Job::new`.
#[derive(Debug, Clone, Copy)]
pub struct Job<'a> {
    pub(crate) client: &'a Client,
}
impl<'a> Job<'a> {
    /// Borrow a client as this operation group. Free — no allocation, no clone.
    pub fn new(client: &'a Client) -> Self {
        Self { client }
    }
    /// Lists all jobs for a repository
    ///
    /// `GET /repos/{owner}/{repo}/actions/jobs` — needs the `read:repository` token scope.
    pub async fn list(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::ListWorkflowJobsQuery,
    ) -> Result<gitea_model::ActionWorkflowJobsResponse> {
        let req = query.apply(
            Request::get(format!(
                "/repos/{}/{}/actions/jobs",
                encode::seg(owner),
                encode::seg(repo)
            ))
            .scope("read:repository"),
        );
        self.client.json(req).await
    }
    /// Downloads the job logs for a workflow run
    ///
    /// `GET /repos/{owner}/{repo}/actions/jobs/{job_id}/logs` — needs the `read:repository` token scope.
    pub async fn logs(&self, owner: &str, repo: &str, job_id: i32) -> Result<(Mime, ByteStream)> {
        let req = Request::get(format!(
            "/repos/{}/{}/actions/jobs/{}/logs",
            encode::seg(owner),
            encode::seg(repo),
            job_id
        ))
        .scope("read:repository")
        .accept(Accept::Any);
        self.client.bytes(req).await
    }
    /// Gets a specific workflow job for a workflow run
    ///
    /// `GET /repos/{owner}/{repo}/actions/jobs/{job_id}` — needs the `read:repository` token scope.
    pub async fn view(
        &self,
        owner: &str,
        repo: &str,
        job_id: &str,
    ) -> Result<gitea_model::ActionWorkflowJob> {
        let req = Request::get(format!(
            "/repos/{}/{}/actions/jobs/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(job_id)
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
}
