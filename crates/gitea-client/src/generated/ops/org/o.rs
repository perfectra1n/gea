// @generated by `cargo xtask codegen` from spec/gitea-1.27.3.json
// (sha256:52a2fbe7f08adfa7). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `org` operations: `o`.
use gitea_core::Result;
use gitea_core::http::{ItemStream, PageInfo, Paging, Request, encode};
impl super::Org<'_> {
    /// Block a user
    ///
    /// `PUT /orgs/{org}/blocks/{username}` — needs the `write:organization` token scope.
    pub async fn organization_block_user(
        &self,
        org: &str,
        username: &str,
        query: &crate::query::OrganizationBlockUserQuery,
    ) -> Result<()> {
        let req = query.apply(
            Request::put(format!("/orgs/{}/blocks/{}", encode::seg(org), encode::seg(username)))
                .scope("write:organization"),
        );
        self.client.empty(req).await
    }
    /// Check if a user is blocked by the organization
    ///
    /// `GET /orgs/{org}/blocks/{username}` — needs the `read:organization` token scope.
    pub async fn organization_check_user_block(&self, org: &str, username: &str) -> Result<()> {
        let req =
            Request::get(format!("/orgs/{}/blocks/{}", encode::seg(org), encode::seg(username)))
                .scope("read:organization");
        self.client.empty(req).await
    }
    /// List users blocked by the organization
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Gitea silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /orgs/{org}/blocks` — needs the `read:organization` token scope.
    pub fn organization_list_blocks(
        &self,
        org: &str,
        query: &crate::query::OrganizationListBlocksQuery,
    ) -> ItemStream<gitea_model::User> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/orgs/{}/blocks", encode::seg(org)))
                    .scope("read:organization"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List users blocked by the organization
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /orgs/{org}/blocks` — needs the `read:organization` token scope.
    pub async fn organization_list_blocks_page(
        &self,
        org: &str,
        query: &crate::query::OrganizationListBlocksQuery,
        paging: Paging,
    ) -> Result<(Vec<gitea_model::User>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/orgs/{}/blocks", encode::seg(org)))
                    .scope("read:organization"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// Unblock a user
    ///
    /// `DELETE /orgs/{org}/blocks/{username}` — needs the `write:organization` token scope.
    pub async fn organization_unblock_user(&self, org: &str, username: &str) -> Result<()> {
        let req =
            Request::delete(format!("/orgs/{}/blocks/{}", encode::seg(org), encode::seg(username)))
                .scope("write:organization");
        self.client.empty(req).await
    }
}
