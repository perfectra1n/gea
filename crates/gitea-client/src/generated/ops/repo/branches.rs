// @generated by `cargo xtask codegen` from spec/gitea-1.27.3.json
// (sha256:52a2fbe7f08adfa7). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `repo` operations: `branches`.
use gitea_core::Result;
use gitea_core::http::{ItemStream, PageInfo, Paging, Request, encode};
impl super::Repo<'_> {
    /// Create a branch
    ///
    /// `POST /repos/{owner}/{repo}/branches` — needs the `write:repository` token scope.
    pub async fn create_branch(
        &self,
        owner: &str,
        repo: &str,
        body: &gitea_model::CreateBranchRepoOption,
    ) -> Result<gitea_model::Branch> {
        let req =
            Request::post(format!("/repos/{}/{}/branches", encode::seg(owner), encode::seg(repo)))
                .scope("write:repository")
                .json_body(body)?;
        self.client.json(req).await
    }
    /// Create a branch protections for a repository
    ///
    /// `POST /repos/{owner}/{repo}/branch_protections` — needs the `write:repository` token scope.
    pub async fn create_branch_protection(
        &self,
        owner: &str,
        repo: &str,
        body: &gitea_model::CreateBranchProtectionOption,
    ) -> Result<gitea_model::BranchProtection> {
        let req = Request::post(format!(
            "/repos/{}/{}/branch_protections",
            encode::seg(owner),
            encode::seg(repo)
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Delete a specific branch from a repository
    ///
    /// `DELETE /repos/{owner}/{repo}/branches/{branch}` — needs the `write:repository` token scope.
    pub async fn delete_branch(&self, owner: &str, repo: &str, branch: &str) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/branches/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(branch)
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Delete a specific branch protection for the repository
    ///
    /// `DELETE /repos/{owner}/{repo}/branch_protections/{name}` — needs the `write:repository` token scope.
    pub async fn delete_branch_protection(
        &self,
        owner: &str,
        repo: &str,
        name: &str,
    ) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/branch_protections/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(name)
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Edit a branch protections for a repository. Only fields that are set will be changed
    ///
    /// `PATCH /repos/{owner}/{repo}/branch_protections/{name}` — needs the `write:repository` token scope.
    pub async fn edit_branch_protection(
        &self,
        owner: &str,
        repo: &str,
        name: &str,
        body: &gitea_model::EditBranchProtectionOption,
    ) -> Result<gitea_model::BranchProtection> {
        let req = Request::patch(format!(
            "/repos/{}/{}/branch_protections/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(name)
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Retrieve a specific branch from a repository, including its effective branch protection
    ///
    /// `GET /repos/{owner}/{repo}/branches/{branch}` — needs the `read:repository` token scope.
    pub async fn get_branch(
        &self,
        owner: &str,
        repo: &str,
        branch: &str,
    ) -> Result<gitea_model::Branch> {
        let req = Request::get(format!(
            "/repos/{}/{}/branches/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(branch)
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// Get a specific branch protection for the repository
    ///
    /// `GET /repos/{owner}/{repo}/branch_protections/{name}` — needs the `read:repository` token scope.
    pub async fn get_branch_protection(
        &self,
        owner: &str,
        repo: &str,
        name: &str,
    ) -> Result<gitea_model::BranchProtection> {
        let req = Request::get(format!(
            "/repos/{}/{}/branch_protections/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(name)
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// List branch protections for a repository
    ///
    /// `GET /repos/{owner}/{repo}/branch_protections` — needs the `read:repository` token scope.
    pub async fn list_branch_protection(
        &self,
        owner: &str,
        repo: &str,
    ) -> Result<Vec<gitea_model::BranchProtection>> {
        let req = Request::get(format!(
            "/repos/{}/{}/branch_protections",
            encode::seg(owner),
            encode::seg(repo)
        ))
        .scope("read:repository");
        Ok(self
            .client
            .json::<Option<Vec<gitea_model::BranchProtection>>>(req)
            .await?
            .unwrap_or_default())
    }
    /// List a repository's branches
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Gitea silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/branches` — needs the `read:repository` token scope.
    pub fn list_branches(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoListBranchesQuery,
    ) -> ItemStream<gitea_model::Branch> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/branches",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:repository"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List a repository's branches
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/branches` — needs the `read:repository` token scope.
    pub async fn list_branches_page(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoListBranchesQuery,
        paging: Paging,
    ) -> Result<(Vec<gitea_model::Branch>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/branches",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:repository"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// Rename a branch
    ///
    /// `PATCH /repos/{owner}/{repo}/branches/{branch}` — needs the `write:repository` token scope.
    pub async fn rename_branch(
        &self,
        owner: &str,
        repo: &str,
        branch: &str,
        body: &gitea_model::RenameBranchRepoOption,
    ) -> Result<()> {
        let req = Request::patch(format!(
            "/repos/{}/{}/branches/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(branch)
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.empty(req).await
    }
    /// Update a branch reference to a new commit
    ///
    /// `PUT /repos/{owner}/{repo}/branches/{branch}` — needs the `write:repository` token scope.
    pub async fn update_branch(
        &self,
        owner: &str,
        repo: &str,
        branch: &str,
        body: &gitea_model::UpdateBranchRepoOption,
    ) -> Result<()> {
        let req = Request::put(format!(
            "/repos/{}/{}/branches/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(branch)
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.empty(req).await
    }
    /// Update the priorities of branch protections for a repository.
    ///
    /// `POST /repos/{owner}/{repo}/branch_protections/priority` — needs the `write:repository` token scope.
    pub async fn update_branch_protection_priories(
        &self,
        owner: &str,
        repo: &str,
        body: &gitea_model::UpdateBranchProtectionPriories,
    ) -> Result<()> {
        let req = Request::post(format!(
            "/repos/{}/{}/branch_protections/priority",
            encode::seg(owner),
            encode::seg(repo)
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.empty(req).await
    }
}
