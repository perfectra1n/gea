// @generated by `cargo xtask codegen` from spec/gitea-1.27.3.json
// (sha256:52a2fbe7f08adfa7). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `git` — Git plumbing: blobs, trees, annotated tags.
use gitea_core::Result;
use gitea_core::http::{Client, Request, encode};
/// Git plumbing: blobs, trees, annotated tags.
///
/// Obtain one from `Api::git`, or wrap a bare `Client` with `Git::new`.
#[derive(Debug, Clone, Copy)]
pub struct Git<'a> {
    pub(crate) client: &'a Client,
}
impl<'a> Git<'a> {
    /// Borrow a client as this operation group. Free — no allocation, no clone.
    pub fn new(client: &'a Client) -> Self {
        Self { client }
    }
    /// Gets the tag object of an annotated tag (not lightweight tags)
    ///
    /// `GET /repos/{owner}/{repo}/git/tags/{sha}` — needs the `read:repository` token scope.
    pub async fn annotated_tag(
        &self,
        owner: &str,
        repo: &str,
        sha: &str,
    ) -> Result<gitea_model::AnnotatedTag> {
        let req = Request::get(format!(
            "/repos/{}/{}/git/tags/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(sha)
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// Gets the blob of a repository.
    ///
    /// `GET /repos/{owner}/{repo}/git/blobs/{sha}` — needs the `read:repository` token scope.
    pub async fn blob(
        &self,
        owner: &str,
        repo: &str,
        sha: &str,
    ) -> Result<gitea_model::GitBlobResponse> {
        let req = Request::get(format!(
            "/repos/{}/{}/git/blobs/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(sha)
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// Gets the tree of a repository.
    ///
    /// `GET /repos/{owner}/{repo}/git/trees/{sha}` — needs the `read:repository` token scope.
    pub async fn tree(
        &self,
        owner: &str,
        repo: &str,
        sha: &str,
        query: &crate::query::GetTreeQuery,
    ) -> Result<gitea_model::GitTreeResponse> {
        let req = query.apply(
            Request::get(format!(
                "/repos/{}/{}/git/trees/{}",
                encode::seg(owner),
                encode::seg(repo),
                encode::seg(sha)
            ))
            .scope("read:repository"),
        );
        self.client.json(req).await
    }
}
