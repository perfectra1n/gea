// @generated by `cargo xtask codegen` from spec/gitea-1.27.3.json
// (sha256:52a2fbe7f08adfa7). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `issue` operations: `g`.
use gitea_core::Result;
use gitea_core::http::{ItemStream, PageInfo, Paging, Request, encode};
impl super::Issue<'_> {
    /// Get a comment
    ///
    /// `GET /repos/{owner}/{repo}/issues/comments/{id}` — needs the `read:issue` token scope.
    pub async fn get_comment(
        &self,
        owner: &str,
        repo: &str,
        id: i64,
    ) -> Result<gitea_model::Comment> {
        let req = Request::get(format!(
            "/repos/{}/{}/issues/comments/{}",
            encode::seg(owner),
            encode::seg(repo),
            id
        ))
        .scope("read:issue");
        self.client.json(req).await
    }
    /// Get a list of reactions from a comment of an issue
    ///
    /// `GET /repos/{owner}/{repo}/issues/comments/{id}/reactions` — needs the `read:issue` token scope.
    pub async fn get_comment_reactions(
        &self,
        owner: &str,
        repo: &str,
        id: i64,
    ) -> Result<Vec<gitea_model::Reaction>> {
        let req = Request::get(format!(
            "/repos/{}/{}/issues/comments/{}/reactions",
            encode::seg(owner),
            encode::seg(repo),
            id
        ))
        .scope("read:issue");
        Ok(self.client.json::<Option<Vec<gitea_model::Reaction>>>(req).await?.unwrap_or_default())
    }
    /// List all comments on an issue
    ///
    /// `GET /repos/{owner}/{repo}/issues/{index}/comments` — needs the `read:issue` token scope.
    pub async fn get_comments(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        query: &crate::query::IssueGetCommentsQuery,
    ) -> Result<Vec<gitea_model::Comment>> {
        let req = query.apply(
            Request::get(format!(
                "/repos/{}/{}/issues/{}/comments",
                encode::seg(owner),
                encode::seg(repo),
                index
            ))
            .scope("read:issue"),
        );
        Ok(self.client.json::<Option<Vec<gitea_model::Comment>>>(req).await?.unwrap_or_default())
    }
    /// List all comments and events on an issue
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Gitea silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/issues/{index}/timeline` — needs the `read:issue` token scope.
    pub fn get_comments_and_timeline(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        query: &crate::query::IssueGetCommentsAndTimelineQuery,
    ) -> ItemStream<gitea_model::TimelineComment> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/issues/{}/timeline",
                    encode::seg(owner),
                    encode::seg(repo),
                    index
                ))
                .scope("read:issue"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List all comments and events on an issue
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/issues/{index}/timeline` — needs the `read:issue` token scope.
    pub async fn get_comments_and_timeline_page(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        query: &crate::query::IssueGetCommentsAndTimelineQuery,
        paging: Paging,
    ) -> Result<(Vec<gitea_model::TimelineComment>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/issues/{}/timeline",
                    encode::seg(owner),
                    encode::seg(repo),
                    index
                ))
                .scope("read:issue"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// Get an issue
    ///
    /// `GET /repos/{owner}/{repo}/issues/{index}` — needs the `read:issue` token scope.
    pub async fn get_issue(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
    ) -> Result<gitea_model::Issue> {
        let req = Request::get(format!(
            "/repos/{}/{}/issues/{}",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("read:issue");
        self.client.json(req).await
    }
    /// Get an issue attachment
    ///
    /// `GET /repos/{owner}/{repo}/issues/{index}/assets/{attachment_id}` — needs the `read:issue` token scope.
    pub async fn get_issue_attachment(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        attachment_id: i64,
    ) -> Result<gitea_model::Attachment> {
        let req = Request::get(format!(
            "/repos/{}/{}/issues/{}/assets/{}",
            encode::seg(owner),
            encode::seg(repo),
            index,
            attachment_id
        ))
        .scope("read:issue");
        self.client.json(req).await
    }
    /// Get a comment attachment
    ///
    /// `GET /repos/{owner}/{repo}/issues/comments/{id}/assets/{attachment_id}` — needs the `read:issue` token scope.
    pub async fn get_issue_comment_attachment(
        &self,
        owner: &str,
        repo: &str,
        id: i64,
        attachment_id: i64,
    ) -> Result<gitea_model::Attachment> {
        let req = Request::get(format!(
            "/repos/{}/{}/issues/comments/{}/assets/{}",
            encode::seg(owner),
            encode::seg(repo),
            id,
            attachment_id
        ))
        .scope("read:issue");
        self.client.json(req).await
    }
    /// Get a list reactions of an issue
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Gitea silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/issues/{index}/reactions` — needs the `read:issue` token scope.
    pub fn get_issue_reactions(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        query: &crate::query::IssueGetIssueReactionsQuery,
    ) -> ItemStream<gitea_model::Reaction> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/issues/{}/reactions",
                    encode::seg(owner),
                    encode::seg(repo),
                    index
                ))
                .scope("read:issue"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// Get a list reactions of an issue
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/issues/{index}/reactions` — needs the `read:issue` token scope.
    pub async fn get_issue_reactions_page(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        query: &crate::query::IssueGetIssueReactionsQuery,
        paging: Paging,
    ) -> Result<(Vec<gitea_model::Reaction>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/issues/{}/reactions",
                    encode::seg(owner),
                    encode::seg(repo),
                    index
                ))
                .scope("read:issue"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// Get a single label
    ///
    /// `GET /repos/{owner}/{repo}/labels/{id}` — needs the `read:issue` token scope.
    pub async fn get_label(&self, owner: &str, repo: &str, id: i64) -> Result<gitea_model::Label> {
        let req = Request::get(format!(
            "/repos/{}/{}/labels/{}",
            encode::seg(owner),
            encode::seg(repo),
            id
        ))
        .scope("read:issue");
        self.client.json(req).await
    }
    /// Get an issue's labels
    ///
    /// `GET /repos/{owner}/{repo}/issues/{index}/labels` — needs the `read:issue` token scope.
    pub async fn get_labels(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
    ) -> Result<Vec<gitea_model::Label>> {
        let req = Request::get(format!(
            "/repos/{}/{}/issues/{}/labels",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("read:issue");
        Ok(self.client.json::<Option<Vec<gitea_model::Label>>>(req).await?.unwrap_or_default())
    }
    /// Get a milestone
    ///
    /// `GET /repos/{owner}/{repo}/milestones/{id}` — needs the `read:issue` token scope.
    pub async fn get_milestone(
        &self,
        owner: &str,
        repo: &str,
        id: &str,
    ) -> Result<gitea_model::Milestone> {
        let req = Request::get(format!(
            "/repos/{}/{}/milestones/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(id)
        ))
        .scope("read:issue");
        self.client.json(req).await
    }
    /// Get all of a repository's opened milestones
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Gitea silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/milestones` — needs the `read:issue` token scope.
    pub fn get_milestones_list(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::IssueGetMilestonesListQuery,
    ) -> ItemStream<gitea_model::Milestone> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/milestones",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:issue"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// Get all of a repository's opened milestones
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/milestones` — needs the `read:issue` token scope.
    pub async fn get_milestones_list_page(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::IssueGetMilestonesListQuery,
        paging: Paging,
    ) -> Result<(Vec<gitea_model::Milestone>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/milestones",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:issue"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List all comments in a repository
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Gitea silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/issues/comments` — needs the `read:issue` token scope.
    pub fn get_repo_comments(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::IssueGetRepoCommentsQuery,
    ) -> ItemStream<gitea_model::Comment> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/issues/comments",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:issue"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List all comments in a repository
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/issues/comments` — needs the `read:issue` token scope.
    pub async fn get_repo_comments_page(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::IssueGetRepoCommentsQuery,
        paging: Paging,
    ) -> Result<(Vec<gitea_model::Comment>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/issues/comments",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:issue"),
            ),
            paging,
        );
        self.client.page(req).await
    }
}
