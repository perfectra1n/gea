// @generated by `cargo xtask codegen` from spec/gitea-1.27.3.json
// (sha256:52a2fbe7f08adfa7). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `repo` operations: `collaborators`.
use gitea_core::Result;
use gitea_core::http::{ItemStream, PageInfo, Paging, Request, encode};
impl super::Repo<'_> {
    /// Add or Update a collaborator to a repository
    ///
    /// `PUT /repos/{owner}/{repo}/collaborators/{collaborator}` — needs the `write:repository` token scope.
    pub async fn add_collaborator(
        &self,
        owner: &str,
        repo: &str,
        collaborator: &str,
        body: &gitea_model::AddCollaboratorOption,
    ) -> Result<()> {
        let req = Request::put(format!(
            "/repos/{}/{}/collaborators/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(collaborator)
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.empty(req).await
    }
    /// Add a team to a repository
    ///
    /// `PUT /repos/{owner}/{repo}/teams/{team}` — needs the `write:repository` token scope.
    pub async fn add_team(&self, owner: &str, repo: &str, team: &str) -> Result<()> {
        let req = Request::put(format!(
            "/repos/{}/{}/teams/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(team)
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Add a topic to a repository
    ///
    /// `PUT /repos/{owner}/{repo}/topics/{topic}` — needs the `write:repository` token scope.
    pub async fn add_topic(&self, owner: &str, repo: &str, topic: &str) -> Result<()> {
        let req = Request::put(format!(
            "/repos/{}/{}/topics/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(topic)
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Check if a user can be assigned to issues in a repository
    ///
    /// `GET /repos/{owner}/{repo}/assignees/{assignee}` — needs the `read:repository` token scope.
    pub async fn check_assignee(&self, owner: &str, repo: &str, assignee: &str) -> Result<()> {
        let req = Request::get(format!(
            "/repos/{}/{}/assignees/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(assignee)
        ))
        .scope("read:repository");
        self.client.empty(req).await
    }
    /// Check if a user is a collaborator of a repository
    ///
    /// `GET /repos/{owner}/{repo}/collaborators/{collaborator}` — needs the `read:repository` token scope.
    pub async fn check_collaborator(
        &self,
        owner: &str,
        repo: &str,
        collaborator: &str,
    ) -> Result<()> {
        let req = Request::get(format!(
            "/repos/{}/{}/collaborators/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(collaborator)
        ))
        .scope("read:repository");
        self.client.empty(req).await
    }
    /// Check if a team is assigned to a repository
    ///
    /// `GET /repos/{owner}/{repo}/teams/{team}` — needs the `read:repository` token scope.
    pub async fn check_team(
        &self,
        owner: &str,
        repo: &str,
        team: &str,
    ) -> Result<gitea_model::Team> {
        let req = Request::get(format!(
            "/repos/{}/{}/teams/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(team)
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// Delete a collaborator from a repository
    ///
    /// `DELETE /repos/{owner}/{repo}/collaborators/{collaborator}` — needs the `write:repository` token scope.
    pub async fn delete_collaborator(
        &self,
        owner: &str,
        repo: &str,
        collaborator: &str,
    ) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/collaborators/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(collaborator)
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Delete a team from a repository
    ///
    /// `DELETE /repos/{owner}/{repo}/teams/{team}` — needs the `write:repository` token scope.
    pub async fn delete_team(&self, owner: &str, repo: &str, team: &str) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/teams/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(team)
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Delete a topic from a repository
    ///
    /// `DELETE /repos/{owner}/{repo}/topics/{topic}` — needs the `write:repository` token scope.
    pub async fn delete_topic(&self, owner: &str, repo: &str, topic: &str) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/topics/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(topic)
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Return all users that have write access and can be assigned to issues
    ///
    /// `GET /repos/{owner}/{repo}/assignees` — needs the `read:repository` token scope.
    pub async fn get_assignees(&self, owner: &str, repo: &str) -> Result<Vec<gitea_model::User>> {
        let req =
            Request::get(format!("/repos/{}/{}/assignees", encode::seg(owner), encode::seg(repo)))
                .scope("read:repository");
        Ok(self.client.json::<Option<Vec<gitea_model::User>>>(req).await?.unwrap_or_default())
    }
    /// Get repository permissions for a user
    ///
    /// `GET /repos/{owner}/{repo}/collaborators/{collaborator}/permission` — needs the `read:repository` token scope.
    pub async fn get_repo_permissions(
        &self,
        owner: &str,
        repo: &str,
        collaborator: &str,
    ) -> Result<gitea_model::RepoCollaboratorPermission> {
        let req = Request::get(format!(
            "/repos/{}/{}/collaborators/{}/permission",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(collaborator)
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// Return all users that can be requested to review in this repo
    ///
    /// `GET /repos/{owner}/{repo}/reviewers` — needs the `read:repository` token scope.
    pub async fn get_reviewers(&self, owner: &str, repo: &str) -> Result<Vec<gitea_model::User>> {
        let req =
            Request::get(format!("/repos/{}/{}/reviewers", encode::seg(owner), encode::seg(repo)))
                .scope("read:repository");
        Ok(self.client.json::<Option<Vec<gitea_model::User>>>(req).await?.unwrap_or_default())
    }
    /// List a repository's collaborators
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Gitea silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/collaborators` — needs the `read:repository` token scope.
    pub fn list_collaborators(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoListCollaboratorsQuery,
    ) -> ItemStream<gitea_model::User> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/collaborators",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:repository"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List a repository's collaborators
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/collaborators` — needs the `read:repository` token scope.
    pub async fn list_collaborators_page(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoListCollaboratorsQuery,
        paging: Paging,
    ) -> Result<(Vec<gitea_model::User>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/collaborators",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:repository"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List a repo's watchers
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Gitea silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/subscribers` — needs the `read:repository` token scope.
    pub fn list_subscribers(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoListSubscribersQuery,
    ) -> ItemStream<gitea_model::User> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/subscribers",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:repository"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List a repo's watchers
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/subscribers` — needs the `read:repository` token scope.
    pub async fn list_subscribers_page(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoListSubscribersQuery,
        paging: Paging,
    ) -> Result<(Vec<gitea_model::User>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/subscribers",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:repository"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// List a repository's teams
    ///
    /// `GET /repos/{owner}/{repo}/teams` — needs the `read:repository` token scope.
    pub async fn list_teams(&self, owner: &str, repo: &str) -> Result<Vec<gitea_model::Team>> {
        let req =
            Request::get(format!("/repos/{}/{}/teams", encode::seg(owner), encode::seg(repo)))
                .scope("read:repository");
        Ok(self.client.json::<Option<Vec<gitea_model::Team>>>(req).await?.unwrap_or_default())
    }
    /// Get list of topics that a repository has
    ///
    /// `GET /repos/{owner}/{repo}/topics` — needs the `read:repository` token scope.
    pub async fn list_topics(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoListTopicsQuery,
    ) -> Result<gitea_model::TopicName> {
        let req = query.apply(
            Request::get(format!("/repos/{}/{}/topics", encode::seg(owner), encode::seg(repo)))
                .scope("read:repository"),
        );
        self.client.json(req).await
    }
    /// Replace list of topics for a repository
    ///
    /// `PUT /repos/{owner}/{repo}/topics` — needs the `write:repository` token scope.
    pub async fn update_topics(
        &self,
        owner: &str,
        repo: &str,
        body: &gitea_model::RepoTopicOptions,
    ) -> Result<()> {
        let req =
            Request::put(format!("/repos/{}/{}/topics", encode::seg(owner), encode::seg(repo)))
                .scope("write:repository")
                .json_body(body)?;
        self.client.empty(req).await
    }
}
