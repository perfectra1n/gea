// @generated by `cargo xtask codegen` from spec/gitea-1.27.3.json
// (sha256:52a2fbe7f08adfa7). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `issue` operations: `r`.
use gitea_core::Result;
use gitea_core::http::{Request, encode};
impl super::Issue<'_> {
    /// Remove assignees from an issue
    ///
    /// `DELETE /repos/{owner}/{repo}/issues/{index}/assignees` — needs the `write:issue` token scope.
    pub async fn remove_assignees(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        body: &gitea_model::IssueAssigneesOption,
    ) -> Result<gitea_model::Issue> {
        let req = Request::delete(format!(
            "/repos/{}/{}/issues/{}/assignees",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("write:issue")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Unblock the issue given in the body by the issue in path
    ///
    /// `DELETE /repos/{owner}/{repo}/issues/{index}/blocks` — needs the `write:issue` token scope.
    pub async fn remove_issue_blocking(
        &self,
        owner: &str,
        repo: &str,
        index: &str,
        body: &gitea_model::IssueMeta,
    ) -> Result<gitea_model::Issue> {
        let req = Request::delete(format!(
            "/repos/{}/{}/issues/{}/blocks",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(index)
        ))
        .scope("write:issue")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Remove an issue dependency
    ///
    /// `DELETE /repos/{owner}/{repo}/issues/{index}/dependencies` — needs the `write:issue` token scope.
    pub async fn remove_issue_dependencies(
        &self,
        owner: &str,
        repo: &str,
        index: &str,
        body: &gitea_model::IssueMeta,
    ) -> Result<gitea_model::Issue> {
        let req = Request::delete(format!(
            "/repos/{}/{}/issues/{}/dependencies",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(index)
        ))
        .scope("write:issue")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Remove a label from an issue
    ///
    /// `DELETE /repos/{owner}/{repo}/issues/{index}/labels/{id}` — needs the `write:issue` token scope.
    pub async fn remove_label(&self, owner: &str, repo: &str, index: i64, id: i64) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/issues/{}/labels/{}",
            encode::seg(owner),
            encode::seg(repo),
            index,
            id
        ))
        .scope("write:issue");
        self.client.empty(req).await
    }
    /// Replace an issue's labels
    ///
    /// `PUT /repos/{owner}/{repo}/issues/{index}/labels` — needs the `write:issue` token scope.
    pub async fn replace_labels(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        body: &gitea_model::IssueLabelsOption,
    ) -> Result<Vec<gitea_model::Label>> {
        let req = Request::put(format!(
            "/repos/{}/{}/issues/{}/labels",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("write:issue")
        .json_body(body)?;
        Ok(self.client.json::<Option<Vec<gitea_model::Label>>>(req).await?.unwrap_or_default())
    }
    /// Reset a tracked time of an issue
    ///
    /// `DELETE /repos/{owner}/{repo}/issues/{index}/times` — needs the `write:issue` token scope.
    pub async fn reset_time(&self, owner: &str, repo: &str, index: i64) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/issues/{}/times",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("write:issue");
        self.client.empty(req).await
    }
}
