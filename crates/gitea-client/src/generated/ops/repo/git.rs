// @generated by `cargo xtask codegen` from spec/gitea-1.27.3.json
// (sha256:52a2fbe7f08adfa7). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `repo` operations: `git`.
use gitea_core::Result;
use gitea_core::http::{Accept, ItemStream, PageInfo, Paging, Request, encode};
impl super::Repo<'_> {
    /// Get commit comparison information By default returns JSON commit comparison information. The raw
    /// diff or patch can be requested with the `output` query parameter set to `diff` or `patch`
    /// respectively.
    ///
    /// `GET /repos/{owner}/{repo}/compare/{basehead}` — needs the `read:repository` token scope.
    pub async fn compare_diff(
        &self,
        owner: &str,
        repo: &str,
        basehead: &str,
        query: &crate::query::RepoCompareDiffQuery,
    ) -> Result<gitea_model::Compare> {
        let req = query.apply(
            Request::get(format!(
                "/repos/{}/{}/compare/{}",
                encode::seg(owner),
                encode::seg(repo),
                encode::seg(basehead)
            ))
            .scope("read:repository"),
        );
        self.client.json(req).await
    }
    /// Add a key to a repository
    ///
    /// `POST /repos/{owner}/{repo}/keys` — needs the `write:repository` token scope.
    pub async fn create_key(
        &self,
        owner: &str,
        repo: &str,
        body: &gitea_model::CreateKeyOption,
    ) -> Result<gitea_model::DeployKey> {
        let req =
            Request::post(format!("/repos/{}/{}/keys", encode::seg(owner), encode::seg(repo)))
                .scope("write:repository")
                .json_body(body)?;
        self.client.json(req).await
    }
    /// Delete a key from a repository
    ///
    /// `DELETE /repos/{owner}/{repo}/keys/{id}` — needs the `write:repository` token scope.
    pub async fn delete_key(&self, owner: &str, repo: &str, id: i64) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/keys/{}",
            encode::seg(owner),
            encode::seg(repo),
            id
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Get a commit's diff or patch
    ///
    /// `GET /repos/{owner}/{repo}/git/commits/{sha}.{diffType}` — needs the `read:repository` token scope.
    pub async fn download_commit_diff_or_patch(
        &self,
        owner: &str,
        repo: &str,
        sha: &str,
        diff_type: &str,
    ) -> Result<String> {
        let req = Request::get(format!(
            "/repos/{}/{}/git/commits/{}.{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(sha),
            encode::seg(diff_type)
        ))
        .scope("read:repository")
        .accept(Accept::Text);
        crate::text(self.client, req).await
    }
    /// Get a list of all commits from a repository
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Gitea silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/commits` — needs the `read:repository` token scope.
    pub fn get_all_commits(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoGetAllCommitsQuery,
    ) -> ItemStream<gitea_model::Commit> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/commits",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:repository"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// Get a list of all commits from a repository
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/commits` — needs the `read:repository` token scope.
    pub async fn get_all_commits_page(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoGetAllCommitsQuery,
        paging: Paging,
    ) -> Result<(Vec<gitea_model::Commit>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/commits",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:repository"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// Get a commit's combined status, by branch/tag/commit reference
    ///
    /// `GET /repos/{owner}/{repo}/commits/{ref}/status` — needs the `read:repository` token scope.
    pub async fn get_combined_status_by_ref(
        &self,
        owner: &str,
        repo: &str,
        r#ref: &str,
        query: &crate::query::RepoGetCombinedStatusByRefQuery,
    ) -> Result<gitea_model::CombinedStatus> {
        let req = query.apply(
            Request::get(format!(
                "/repos/{}/{}/commits/{}/status",
                encode::seg(owner),
                encode::seg(repo),
                encode::path_like(r#ref)
            ))
            .scope("read:repository"),
        );
        self.client.json(req).await
    }
    /// Get the merged pull request of the commit
    ///
    /// `GET /repos/{owner}/{repo}/commits/{sha}/pull` — needs the `read:repository` token scope.
    pub async fn get_commit_pull_request(
        &self,
        owner: &str,
        repo: &str,
        sha: &str,
    ) -> Result<gitea_model::PullRequest> {
        let req = Request::get(format!(
            "/repos/{}/{}/commits/{}/pull",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(sha)
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// Get a repository's key by id
    ///
    /// `GET /repos/{owner}/{repo}/keys/{id}` — needs the `read:repository` token scope.
    pub async fn get_key(
        &self,
        owner: &str,
        repo: &str,
        id: i64,
    ) -> Result<gitea_model::DeployKey> {
        let req = Request::get(format!(
            "/repos/{}/{}/keys/{}",
            encode::seg(owner),
            encode::seg(repo),
            id
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// Get a note corresponding to a single commit from a repository
    ///
    /// `GET /repos/{owner}/{repo}/git/notes/{sha}` — needs the `read:repository` token scope.
    pub async fn get_note(
        &self,
        owner: &str,
        repo: &str,
        sha: &str,
        query: &crate::query::RepoGetNoteQuery,
    ) -> Result<gitea_model::Note> {
        let req = query.apply(
            Request::get(format!(
                "/repos/{}/{}/git/notes/{}",
                encode::seg(owner),
                encode::seg(repo),
                encode::seg(sha)
            ))
            .scope("read:repository"),
        );
        self.client.json(req).await
    }
    /// Get a single commit from a repository
    ///
    /// `GET /repos/{owner}/{repo}/git/commits/{sha}` — needs the `read:repository` token scope.
    pub async fn get_single_commit(
        &self,
        owner: &str,
        repo: &str,
        sha: &str,
        query: &crate::query::RepoGetSingleCommitQuery,
    ) -> Result<gitea_model::Commit> {
        let req = query.apply(
            Request::get(format!(
                "/repos/{}/{}/git/commits/{}",
                encode::seg(owner),
                encode::seg(repo),
                encode::seg(sha)
            ))
            .scope("read:repository"),
        );
        self.client.json(req).await
    }
    /// Get specified ref or filtered repository's refs
    ///
    /// `GET /repos/{owner}/{repo}/git/refs` — needs the `read:repository` token scope.
    pub async fn list_all_git_refs(
        &self,
        owner: &str,
        repo: &str,
    ) -> Result<Vec<gitea_model::Reference>> {
        let req =
            Request::get(format!("/repos/{}/{}/git/refs", encode::seg(owner), encode::seg(repo)))
                .scope("read:repository");
        Ok(self.client.json::<Option<Vec<gitea_model::Reference>>>(req).await?.unwrap_or_default())
    }
    /// Get specified ref or filtered repository's refs
    ///
    /// `GET /repos/{owner}/{repo}/git/refs/{ref}` — needs the `read:repository` token scope.
    pub async fn list_git_refs(
        &self,
        owner: &str,
        repo: &str,
        r#ref: &str,
    ) -> Result<Vec<gitea_model::Reference>> {
        let req = Request::get(format!(
            "/repos/{}/{}/git/refs/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::path_like(r#ref)
        ))
        .scope("read:repository");
        Ok(self.client.json::<Option<Vec<gitea_model::Reference>>>(req).await?.unwrap_or_default())
    }
    /// List a repository's keys
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Gitea silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/keys` — needs the `read:repository` token scope.
    pub fn list_keys(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoListKeysQuery,
    ) -> ItemStream<gitea_model::DeployKey> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/repos/{}/{}/keys", encode::seg(owner), encode::seg(repo)))
                    .scope("read:repository"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List a repository's keys
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/keys` — needs the `read:repository` token scope.
    pub async fn list_keys_page(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoListKeysQuery,
        paging: Paging,
    ) -> Result<(Vec<gitea_model::DeployKey>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/repos/{}/{}/keys", encode::seg(owner), encode::seg(repo)))
                    .scope("read:repository"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// Get a commit's statuses, by branch/tag/commit reference
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Gitea silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/commits/{ref}/statuses` — needs the `read:repository` token scope.
    pub fn list_statuses_by_ref(
        &self,
        owner: &str,
        repo: &str,
        r#ref: &str,
        query: &crate::query::RepoListStatusesByRefQuery,
    ) -> ItemStream<gitea_model::CommitStatus> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/commits/{}/statuses",
                    encode::seg(owner),
                    encode::seg(repo),
                    encode::path_like(r#ref)
                ))
                .scope("read:repository"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// Get a commit's statuses, by branch/tag/commit reference
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/commits/{ref}/statuses` — needs the `read:repository` token scope.
    pub async fn list_statuses_by_ref_page(
        &self,
        owner: &str,
        repo: &str,
        r#ref: &str,
        query: &crate::query::RepoListStatusesByRefQuery,
        paging: Paging,
    ) -> Result<(Vec<gitea_model::CommitStatus>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/commits/{}/statuses",
                    encode::seg(owner),
                    encode::seg(repo),
                    encode::path_like(r#ref)
                ))
                .scope("read:repository"),
            ),
            paging,
        );
        self.client.page(req).await
    }
}
