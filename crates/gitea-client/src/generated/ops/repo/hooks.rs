// @generated by `cargo xtask codegen` from spec/gitea-1.27.3.json
// (sha256:52a2fbe7f08adfa7). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `repo` operations: `hooks`.
use gitea_core::Result;
use gitea_core::http::{ItemStream, PageInfo, Paging, Request, encode};
impl super::Repo<'_> {
    /// Create a hook
    ///
    /// `POST /repos/{owner}/{repo}/hooks` — needs the `write:repository` token scope.
    pub async fn create_hook(
        &self,
        owner: &str,
        repo: &str,
        body: &gitea_model::CreateHookOption,
    ) -> Result<gitea_model::Hook> {
        let req =
            Request::post(format!("/repos/{}/{}/hooks", encode::seg(owner), encode::seg(repo)))
                .scope("write:repository")
                .json_body(body)?;
        self.client.json(req).await
    }
    /// Delete a Git hook in a repository
    ///
    /// `DELETE /repos/{owner}/{repo}/hooks/git/{id}` — needs the `write:repository` token scope.
    pub async fn delete_git_hook(&self, owner: &str, repo: &str, id: &str) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/hooks/git/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(id)
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Delete a hook in a repository
    ///
    /// `DELETE /repos/{owner}/{repo}/hooks/{id}` — needs the `write:repository` token scope.
    pub async fn delete_hook(&self, owner: &str, repo: &str, id: i64) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/hooks/{}",
            encode::seg(owner),
            encode::seg(repo),
            id
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Edit a Git hook in a repository
    ///
    /// `PATCH /repos/{owner}/{repo}/hooks/git/{id}` — needs the `write:repository` token scope.
    pub async fn edit_git_hook(
        &self,
        owner: &str,
        repo: &str,
        id: &str,
        body: &gitea_model::EditGitHookOption,
    ) -> Result<gitea_model::GitHook> {
        let req = Request::patch(format!(
            "/repos/{}/{}/hooks/git/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(id)
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Edit a hook in a repository
    ///
    /// `PATCH /repos/{owner}/{repo}/hooks/{id}` — needs the `write:repository` token scope.
    pub async fn edit_hook(
        &self,
        owner: &str,
        repo: &str,
        id: i64,
        body: &gitea_model::EditHookOption,
    ) -> Result<gitea_model::Hook> {
        let req = Request::patch(format!(
            "/repos/{}/{}/hooks/{}",
            encode::seg(owner),
            encode::seg(repo),
            id
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Get a Git hook
    ///
    /// `GET /repos/{owner}/{repo}/hooks/git/{id}` — needs the `read:repository` token scope.
    pub async fn get_git_hook(
        &self,
        owner: &str,
        repo: &str,
        id: &str,
    ) -> Result<gitea_model::GitHook> {
        let req = Request::get(format!(
            "/repos/{}/{}/hooks/git/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(id)
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// Get a hook
    ///
    /// `GET /repos/{owner}/{repo}/hooks/{id}` — needs the `read:repository` token scope.
    pub async fn get_hook(&self, owner: &str, repo: &str, id: i64) -> Result<gitea_model::Hook> {
        let req = Request::get(format!(
            "/repos/{}/{}/hooks/{}",
            encode::seg(owner),
            encode::seg(repo),
            id
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// List the Git hooks in a repository
    ///
    /// `GET /repos/{owner}/{repo}/hooks/git` — needs the `read:repository` token scope.
    pub async fn list_git_hooks(
        &self,
        owner: &str,
        repo: &str,
    ) -> Result<Vec<gitea_model::GitHook>> {
        let req =
            Request::get(format!("/repos/{}/{}/hooks/git", encode::seg(owner), encode::seg(repo)))
                .scope("read:repository");
        Ok(self.client.json::<Option<Vec<gitea_model::GitHook>>>(req).await?.unwrap_or_default())
    }
    /// List the hooks in a repository
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Gitea silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/hooks` — needs the `read:repository` token scope.
    pub fn list_hooks(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoListHooksQuery,
    ) -> ItemStream<gitea_model::Hook> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/repos/{}/{}/hooks", encode::seg(owner), encode::seg(repo)))
                    .scope("read:repository"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List the hooks in a repository
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/hooks` — needs the `read:repository` token scope.
    pub async fn list_hooks_page(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoListHooksQuery,
        paging: Paging,
    ) -> Result<(Vec<gitea_model::Hook>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/repos/{}/{}/hooks", encode::seg(owner), encode::seg(repo)))
                    .scope("read:repository"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// Test a push webhook
    ///
    /// `POST /repos/{owner}/{repo}/hooks/{id}/tests` — needs the `write:repository` token scope.
    pub async fn test_hook(
        &self,
        owner: &str,
        repo: &str,
        id: i64,
        query: &crate::query::RepoTestHookQuery,
    ) -> Result<()> {
        let req = query.apply(
            Request::post(format!(
                "/repos/{}/{}/hooks/{}/tests",
                encode::seg(owner),
                encode::seg(repo),
                id
            ))
            .scope("write:repository"),
        );
        self.client.empty(req).await
    }
}
