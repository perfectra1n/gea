// @generated by `cargo xtask codegen` from spec/gitea-1.27.3.json
// (sha256:52a2fbe7f08adfa7). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `repo` operations: `wiki`.
use gitea_core::Result;
use gitea_core::http::{ItemStream, PageInfo, Paging, Request, encode};
impl super::Repo<'_> {
    /// Create a wiki page
    ///
    /// `POST /repos/{owner}/{repo}/wiki/new` — needs the `write:repository` token scope.
    pub async fn create_wiki_page(
        &self,
        owner: &str,
        repo: &str,
        body: &gitea_model::CreateWikiPageOptions,
    ) -> Result<gitea_model::WikiPage> {
        let req =
            Request::post(format!("/repos/{}/{}/wiki/new", encode::seg(owner), encode::seg(repo)))
                .scope("write:repository")
                .json_body(body)?;
        self.client.json(req).await
    }
    /// Delete a wiki page
    ///
    /// `DELETE /repos/{owner}/{repo}/wiki/page/{pageName}` — needs the `write:repository` token scope.
    pub async fn delete_wiki_page(&self, owner: &str, repo: &str, page_name: &str) -> Result<()> {
        let req = Request::delete(format!(
            "/repos/{}/{}/wiki/page/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(page_name)
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Edit a wiki page
    ///
    /// `PATCH /repos/{owner}/{repo}/wiki/page/{pageName}` — needs the `write:repository` token scope.
    pub async fn edit_wiki_page(
        &self,
        owner: &str,
        repo: &str,
        page_name: &str,
        body: &gitea_model::CreateWikiPageOptions,
    ) -> Result<gitea_model::WikiPage> {
        let req = Request::patch(format!(
            "/repos/{}/{}/wiki/page/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(page_name)
        ))
        .scope("write:repository")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Get a wiki page
    ///
    /// `GET /repos/{owner}/{repo}/wiki/page/{pageName}` — needs the `read:repository` token scope.
    pub async fn get_wiki_page(
        &self,
        owner: &str,
        repo: &str,
        page_name: &str,
    ) -> Result<gitea_model::WikiPage> {
        let req = Request::get(format!(
            "/repos/{}/{}/wiki/page/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(page_name)
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// Get revisions of a wiki page
    ///
    /// `GET /repos/{owner}/{repo}/wiki/revisions/{pageName}` — needs the `read:repository` token scope.
    pub async fn get_wiki_page_revisions(
        &self,
        owner: &str,
        repo: &str,
        page_name: &str,
        query: &crate::query::RepoGetWikiPageRevisionsQuery,
    ) -> Result<gitea_model::WikiCommitList> {
        let req = query.apply(
            Request::get(format!(
                "/repos/{}/{}/wiki/revisions/{}",
                encode::seg(owner),
                encode::seg(repo),
                encode::seg(page_name)
            ))
            .scope("read:repository"),
        );
        self.client.json(req).await
    }
    /// Get all wiki pages
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Gitea silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /repos/{owner}/{repo}/wiki/pages` — needs the `read:repository` token scope.
    pub fn get_wiki_pages(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoGetWikiPagesQuery,
    ) -> ItemStream<gitea_model::WikiPageMetaData> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/wiki/pages",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:repository"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// Get all wiki pages
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /repos/{owner}/{repo}/wiki/pages` — needs the `read:repository` token scope.
    pub async fn get_wiki_pages_page(
        &self,
        owner: &str,
        repo: &str,
        query: &crate::query::RepoGetWikiPagesQuery,
        paging: Paging,
    ) -> Result<(Vec<gitea_model::WikiPageMetaData>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/repos/{}/{}/wiki/pages",
                    encode::seg(owner),
                    encode::seg(repo)
                ))
                .scope("read:repository"),
            ),
            paging,
        );
        self.client.page(req).await
    }
}
