// @generated by `cargo xtask codegen` from spec/gitea-1.27.3.json
// (sha256:52a2fbe7f08adfa7). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `issue` operations: `a`.
use gitea_core::Result;
use gitea_core::http::{Request, encode};
impl super::Issue<'_> {
    /// Add assignees to an issue
    ///
    /// `POST /repos/{owner}/{repo}/issues/{index}/assignees` — needs the `write:issue` token scope.
    pub async fn add_assignees(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        body: &gitea_model::IssueAssigneesOption,
    ) -> Result<gitea_model::Issue> {
        let req = Request::post(format!(
            "/repos/{}/{}/issues/{}/assignees",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("write:issue")
        .json_body(body)?;
        self.client.json(req).await
    }
    /// Add a label to an issue
    ///
    /// `POST /repos/{owner}/{repo}/issues/{index}/labels` — needs the `write:issue` token scope.
    pub async fn add_label(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        body: &gitea_model::IssueLabelsOption,
    ) -> Result<Vec<gitea_model::Label>> {
        let req = Request::post(format!(
            "/repos/{}/{}/issues/{}/labels",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("write:issue")
        .json_body(body)?;
        Ok(self.client.json::<Option<Vec<gitea_model::Label>>>(req).await?.unwrap_or_default())
    }
    /// Subscribe user to issue
    ///
    /// `PUT /repos/{owner}/{repo}/issues/{index}/subscriptions/{user}` — needs the `write:issue` token scope.
    pub async fn add_subscription(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        user: &str,
    ) -> Result<()> {
        let req = Request::put(format!(
            "/repos/{}/{}/issues/{}/subscriptions/{}",
            encode::seg(owner),
            encode::seg(repo),
            index,
            encode::seg(user)
        ))
        .scope("write:issue");
        self.client.empty(req).await
    }
    /// Add tracked time to a issue
    ///
    /// `POST /repos/{owner}/{repo}/issues/{index}/times` — needs the `write:issue` token scope.
    pub async fn add_time(
        &self,
        owner: &str,
        repo: &str,
        index: i64,
        body: &gitea_model::AddTimeOption,
    ) -> Result<gitea_model::TrackedTime> {
        let req = Request::post(format!(
            "/repos/{}/{}/issues/{}/times",
            encode::seg(owner),
            encode::seg(repo),
            index
        ))
        .scope("write:issue")
        .json_body(body)?;
        self.client.json(req).await
    }
}
