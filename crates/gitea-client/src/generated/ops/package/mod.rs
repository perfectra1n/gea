// @generated by `cargo xtask codegen` from spec/gitea-1.27.3.json
// (sha256:52a2fbe7f08adfa7). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `package` — The package registry, owner-scoped.
use gitea_core::Result;
use gitea_core::http::{Client, ItemStream, PageInfo, Paging, Request, encode};
/// The package registry, owner-scoped.
///
/// Obtain one from `Api::package`, or wrap a bare `Client` with `Package::new`.
#[derive(Debug, Clone, Copy)]
pub struct Package<'a> {
    pub(crate) client: &'a Client,
}
impl<'a> Package<'a> {
    /// Borrow a client as this operation group. Free — no allocation, no clone.
    pub fn new(client: &'a Client) -> Self {
        Self { client }
    }
    /// Delete a package
    ///
    /// `DELETE /packages/{owner}/{type}/{name}` — needs the `write:package` token scope.
    pub async fn delete_package(&self, owner: &str, r#type: &str, name: &str) -> Result<()> {
        let req = Request::delete(format!(
            "/packages/{}/{}/{}",
            encode::seg(owner),
            encode::seg(r#type),
            encode::seg(name)
        ))
        .scope("write:package");
        self.client.empty(req).await
    }
    /// Delete a package version
    ///
    /// `DELETE /packages/{owner}/{type}/{name}/{version}` — needs the `write:package` token scope.
    pub async fn delete_package_version(
        &self,
        owner: &str,
        r#type: &str,
        name: &str,
        version: &str,
    ) -> Result<()> {
        let req = Request::delete(format!(
            "/packages/{}/{}/{}/{}",
            encode::seg(owner),
            encode::seg(r#type),
            encode::seg(name),
            encode::seg(version)
        ))
        .scope("write:package");
        self.client.empty(req).await
    }
    /// Gets the latest version of a package
    ///
    /// `GET /packages/{owner}/{type}/{name}/-/latest` — needs the `read:package` token scope.
    pub async fn get_latest_package_version(
        &self,
        owner: &str,
        r#type: &str,
        name: &str,
    ) -> Result<gitea_model::Package> {
        let req = Request::get(format!(
            "/packages/{}/{}/{}/-/latest",
            encode::seg(owner),
            encode::seg(r#type),
            encode::seg(name)
        ))
        .scope("read:package");
        self.client.json(req).await
    }
    /// Gets a package
    ///
    /// `GET /packages/{owner}/{type}/{name}/{version}` — needs the `read:package` token scope.
    pub async fn get_package(
        &self,
        owner: &str,
        r#type: &str,
        name: &str,
        version: &str,
    ) -> Result<gitea_model::Package> {
        let req = Request::get(format!(
            "/packages/{}/{}/{}/{}",
            encode::seg(owner),
            encode::seg(r#type),
            encode::seg(name),
            encode::seg(version)
        ))
        .scope("read:package");
        self.client.json(req).await
    }
    /// Link a package to a repository
    ///
    /// `POST /packages/{owner}/{type}/{name}/-/link/{repo_name}` — needs the `write:package` token scope.
    pub async fn link_package(
        &self,
        owner: &str,
        r#type: &str,
        name: &str,
        repo_name: &str,
    ) -> Result<()> {
        let req = Request::post(format!(
            "/packages/{}/{}/{}/-/link/{}",
            encode::seg(owner),
            encode::seg(r#type),
            encode::seg(name),
            encode::seg(repo_name)
        ))
        .scope("write:package");
        self.client.empty(req).await
    }
    /// Gets all files of a package
    ///
    /// `GET /packages/{owner}/{type}/{name}/{version}/files` — needs the `read:package` token scope.
    pub async fn list_package_files(
        &self,
        owner: &str,
        r#type: &str,
        name: &str,
        version: &str,
    ) -> Result<Vec<gitea_model::PackageFile>> {
        let req = Request::get(format!(
            "/packages/{}/{}/{}/{}/files",
            encode::seg(owner),
            encode::seg(r#type),
            encode::seg(name),
            encode::seg(version)
        ))
        .scope("read:package");
        Ok(self
            .client
            .json::<Option<Vec<gitea_model::PackageFile>>>(req)
            .await?
            .unwrap_or_default())
    }
    /// Gets all versions of a package
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Gitea silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /packages/{owner}/{type}/{name}` — needs the `read:package` token scope.
    pub fn list_package_versions(
        &self,
        owner: &str,
        r#type: &str,
        name: &str,
        query: &crate::query::ListPackageVersionsQuery,
    ) -> ItemStream<gitea_model::Package> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!(
                    "/packages/{}/{}/{}",
                    encode::seg(owner),
                    encode::seg(r#type),
                    encode::seg(name)
                ))
                .scope("read:package"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// Gets all versions of a package
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /packages/{owner}/{type}/{name}` — needs the `read:package` token scope.
    pub async fn list_package_versions_page(
        &self,
        owner: &str,
        r#type: &str,
        name: &str,
        query: &crate::query::ListPackageVersionsQuery,
        paging: Paging,
    ) -> Result<(Vec<gitea_model::Package>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!(
                    "/packages/{}/{}/{}",
                    encode::seg(owner),
                    encode::seg(r#type),
                    encode::seg(name)
                ))
                .scope("read:package"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// Gets all packages of an owner
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Gitea silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /packages/{owner}` — needs the `read:package` token scope.
    pub fn list_packages(
        &self,
        owner: &str,
        query: &crate::query::ListPackagesQuery,
    ) -> ItemStream<gitea_model::Package> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/packages/{}", encode::seg(owner))).scope("read:package"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// Gets all packages of an owner
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /packages/{owner}` — needs the `read:package` token scope.
    pub async fn list_packages_page(
        &self,
        owner: &str,
        query: &crate::query::ListPackagesQuery,
        paging: Paging,
    ) -> Result<(Vec<gitea_model::Package>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/packages/{}", encode::seg(owner))).scope("read:package"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// Unlink a package from a repository
    ///
    /// `POST /packages/{owner}/{type}/{name}/-/unlink` — needs the `write:package` token scope.
    pub async fn unlink_package(&self, owner: &str, r#type: &str, name: &str) -> Result<()> {
        let req = Request::post(format!(
            "/packages/{}/{}/{}/-/unlink",
            encode::seg(owner),
            encode::seg(r#type),
            encode::seg(name)
        ))
        .scope("write:package");
        self.client.empty(req).await
    }
}
