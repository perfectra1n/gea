// @generated by `cargo xtask codegen` from spec/gitea-1.27.3.json
// (sha256:52a2fbe7f08adfa7). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `user` operations: `g`.
use gitea_core::Result;
use gitea_core::http::{Accept, ItemStream, PageInfo, Paging, Request, encode};
impl super::User<'_> {
    /// Get a user
    ///
    /// `GET /users/{username}` — needs the `read:user` token scope.
    pub async fn get(&self, username: &str) -> Result<gitea_model::User> {
        let req = Request::get(format!("/users/{}", encode::seg(username))).scope("read:user");
        self.client.json(req).await
    }
    /// Get the authenticated user
    ///
    /// `GET /user` — needs the `read:user` token scope.
    pub async fn get_current(&self) -> Result<gitea_model::User> {
        let req = Request::get("/user").scope("read:user");
        self.client.json(req).await
    }
    /// Get a user's heatmap
    ///
    /// `GET /users/{username}/heatmap` — needs the `read:user` token scope.
    pub async fn get_heatmap_data(
        &self,
        username: &str,
    ) -> Result<Vec<gitea_model::UserHeatmapData>> {
        let req =
            Request::get(format!("/users/{}/heatmap", encode::seg(username))).scope("read:user");
        Ok(self
            .client
            .json::<Option<Vec<gitea_model::UserHeatmapData>>>(req)
            .await?
            .unwrap_or_default())
    }
    /// Get a hook
    ///
    /// `GET /user/hooks/{id}` — needs the `read:user` token scope.
    pub async fn get_hook(&self, id: i64) -> Result<gitea_model::Hook> {
        let req = Request::get(format!("/user/hooks/{}", id)).scope("read:user");
        self.client.json(req).await
    }
    /// get an OAuth2 Application
    ///
    /// `GET /user/applications/oauth2/{id}` — needs the `read:user` token scope.
    pub async fn get_oauth2_application(&self, id: i64) -> Result<gitea_model::OAuth2Application> {
        let req = Request::get(format!("/user/applications/oauth2/{}", id)).scope("read:user");
        self.client.json(req).await
    }
    /// Get list of all existing stopwatches
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Gitea silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /user/stopwatches` — needs the `read:user` token scope.
    pub fn get_stop_watches(
        &self,
        query: &crate::query::UserGetStopWatchesQuery,
    ) -> ItemStream<gitea_model::StopWatch> {
        let req = crate::stream_request(
            query.apply(Request::get("/user/stopwatches").scope("read:user")),
            "page",
        );
        self.client.items(req)
    }
    /// Get list of all existing stopwatches
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /user/stopwatches` — needs the `read:user` token scope.
    pub async fn get_stop_watches_page(
        &self,
        query: &crate::query::UserGetStopWatchesQuery,
        paging: Paging,
    ) -> Result<(Vec<gitea_model::StopWatch>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/user/stopwatches").scope("read:user")),
            paging,
        );
        self.client.page(req).await
    }
    /// List the authenticated user's access tokens
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Gitea silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /users/{username}/tokens` — needs the `read:user` token scope.
    pub fn get_tokens(
        &self,
        username: &str,
        query: &crate::query::UserGetTokensQuery,
    ) -> ItemStream<gitea_model::AccessToken> {
        let req = crate::stream_request(
            query.apply(
                Request::get(format!("/users/{}/tokens", encode::seg(username))).scope("read:user"),
            ),
            "page",
        );
        self.client.items(req)
    }
    /// List the authenticated user's access tokens
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /users/{username}/tokens` — needs the `read:user` token scope.
    pub async fn get_tokens_page(
        &self,
        username: &str,
        query: &crate::query::UserGetTokensQuery,
        paging: Paging,
    ) -> Result<(Vec<gitea_model::AccessToken>, PageInfo)> {
        let req = crate::page_request(
            query.apply(
                Request::get(format!("/users/{}/tokens", encode::seg(username))).scope("read:user"),
            ),
            paging,
        );
        self.client.page(req).await
    }
    /// Get a user-level runner
    ///
    /// `GET /user/actions/runners/{runner_id}` — needs the `read:user` token scope.
    pub async fn get_user_runner(&self, runner_id: &str) -> Result<gitea_model::ActionRunner> {
        let req = Request::get(format!("/user/actions/runners/{}", encode::seg(runner_id)))
            .scope("read:user");
        self.client.json(req).await
    }
    /// Get user-level runners
    ///
    /// `GET /user/actions/runners` — needs the `read:user` token scope.
    pub async fn get_user_runners(
        &self,
        query: &crate::query::GetUserRunnersQuery,
    ) -> Result<gitea_model::ActionRunnersResponse> {
        let req = query.apply(Request::get("/user/actions/runners").scope("read:user"));
        self.client.json(req).await
    }
    /// Get user settings
    ///
    /// `GET /user/settings` — needs the `read:user` token scope.
    pub async fn get_user_settings(&self) -> Result<gitea_model::UserSettings> {
        let req = Request::get("/user/settings").scope("read:user");
        self.client.json(req).await
    }
    /// Get a user-level variable which is created by current doer
    ///
    /// `GET /user/actions/variables/{variablename}` — needs the `read:user` token scope.
    pub async fn get_user_variable(
        &self,
        variablename: &str,
    ) -> Result<gitea_model::ActionVariable> {
        let req = Request::get(format!("/user/actions/variables/{}", encode::seg(variablename)))
            .scope("read:user");
        self.client.json(req).await
    }
    /// Get the user-level list of variables which is created by current doer
    ///
    /// Every item, as a stream that follows the collection to its end.
    /// The runtime resolves the page size from `/settings/api` and terminates on the
    /// `Link` header rather than on a short page, because Gitea silently clamps
    /// `limit` to `max_response_items`.
    /// Any page number set on the query struct is ignored here — the walk starts at
    /// the beginning. Use the `_page` method for one specific page, and
    /// `futures::StreamExt::take` to cap how many items you consume.
    ///
    /// `GET /user/actions/variables` — needs the `read:user` token scope.
    pub fn get_user_variables_list(
        &self,
        query: &crate::query::GetUserVariablesListQuery,
    ) -> ItemStream<gitea_model::ActionVariable> {
        let req = crate::stream_request(
            query.apply(Request::get("/user/actions/variables").scope("read:user")),
            "page",
        );
        self.client.items(req)
    }
    /// Get the user-level list of variables which is created by current doer
    ///
    /// One page, plus what its headers said — `x-total-count` and `Link`.
    /// For callers driving pagination themselves; the page number comes from the
    /// query struct, and `Paging` narrows the page size.
    ///
    /// `GET /user/actions/variables` — needs the `read:user` token scope.
    pub async fn get_user_variables_list_page(
        &self,
        query: &crate::query::GetUserVariablesListQuery,
        paging: Paging,
    ) -> Result<(Vec<gitea_model::ActionVariable>, PageInfo)> {
        let req = crate::page_request(
            query.apply(Request::get("/user/actions/variables").scope("read:user")),
            paging,
        );
        self.client.page(req).await
    }
    /// Get workflow jobs
    ///
    /// `GET /user/actions/jobs` — needs the `read:user` token scope.
    pub async fn get_user_workflow_jobs(
        &self,
        query: &crate::query::GetUserWorkflowJobsQuery,
    ) -> Result<gitea_model::ActionWorkflowJobsResponse> {
        let req = query.apply(Request::get("/user/actions/jobs").scope("read:user"));
        self.client.json(req).await
    }
    /// Get workflow runs
    ///
    /// `GET /user/actions/runs` — needs the `read:user` token scope.
    pub async fn get_user_workflow_runs(
        &self,
        query: &crate::query::GetUserWorkflowRunsQuery,
    ) -> Result<gitea_model::ActionWorkflowRunsResponse> {
        let req = query.apply(Request::get("/user/actions/runs").scope("read:user"));
        self.client.json(req).await
    }
    /// Get a Token to verify
    ///
    /// `GET /user/gpg_key_token` — needs the `read:user` token scope.
    pub async fn get_verification_token(&self) -> Result<String> {
        let req = Request::get("/user/gpg_key_token").scope("read:user").accept(Accept::Text);
        crate::text(self.client, req).await
    }
}
