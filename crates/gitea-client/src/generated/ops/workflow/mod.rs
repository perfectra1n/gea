// @generated by `cargo xtask codegen` from spec/gitea-1.27.3.json
// (sha256:52a2fbe7f08adfa7). DO NOT EDIT — your changes will be deleted on the next run.
//
// To change this file, change the generator in crates/xtask/src/emit/ or the spec
// in spec/, then run `cargo xtask codegen`. See CONTRIBUTING.md.

//! `workflow` — Actions workflows.
use gitea_core::Result;
use gitea_core::http::{Client, Request, encode};
/// Actions workflows.
///
/// Obtain one from `Api::workflow`, or wrap a bare `Client` with `Workflow::new`.
#[derive(Debug, Clone, Copy)]
pub struct Workflow<'a> {
    pub(crate) client: &'a Client,
}
impl<'a> Workflow<'a> {
    /// Borrow a client as this operation group. Free — no allocation, no clone.
    pub fn new(client: &'a Client) -> Self {
        Self { client }
    }
    /// Disable a workflow
    ///
    /// `PUT /repos/{owner}/{repo}/actions/workflows/{workflow_id}/disable` — needs the `write:repository` token scope.
    pub async fn disable(&self, owner: &str, repo: &str, workflow_id: &str) -> Result<()> {
        let req = Request::put(format!(
            "/repos/{}/{}/actions/workflows/{}/disable",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(workflow_id)
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// Create a workflow dispatch event
    ///
    /// `POST /repos/{owner}/{repo}/actions/workflows/{workflow_id}/dispatches` — needs the `write:repository` token scope.
    pub async fn dispatch(
        &self,
        owner: &str,
        repo: &str,
        workflow_id: &str,
        body: &gitea_model::CreateActionWorkflowDispatch,
        query: &crate::query::ActionsDispatchWorkflowQuery,
    ) -> Result<gitea_model::RunDetails> {
        let req = query.apply(
            Request::post(format!(
                "/repos/{}/{}/actions/workflows/{}/dispatches",
                encode::seg(owner),
                encode::seg(repo),
                encode::seg(workflow_id)
            ))
            .scope("write:repository")
            .json_body(body)?,
        );
        self.client.json(req).await
    }
    /// Enable a workflow
    ///
    /// `PUT /repos/{owner}/{repo}/actions/workflows/{workflow_id}/enable` — needs the `write:repository` token scope.
    pub async fn enable(&self, owner: &str, repo: &str, workflow_id: &str) -> Result<()> {
        let req = Request::put(format!(
            "/repos/{}/{}/actions/workflows/{}/enable",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(workflow_id)
        ))
        .scope("write:repository");
        self.client.empty(req).await
    }
    /// List repository workflows
    ///
    /// `GET /repos/{owner}/{repo}/actions/workflows` — needs the `read:repository` token scope.
    pub async fn list(
        &self,
        owner: &str,
        repo: &str,
    ) -> Result<gitea_model::ActionWorkflowResponse> {
        let req = Request::get(format!(
            "/repos/{}/{}/actions/workflows",
            encode::seg(owner),
            encode::seg(repo)
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
    /// List runs for a workflow
    ///
    /// `GET /repos/{owner}/{repo}/actions/workflows/{workflow_id}/runs` — needs the `read:repository` token scope.
    pub async fn runs(
        &self,
        owner: &str,
        repo: &str,
        workflow_id: &str,
        query: &crate::query::ActionsListWorkflowRunsQuery,
    ) -> Result<gitea_model::ActionWorkflowRunsResponse> {
        let req = query.apply(
            Request::get(format!(
                "/repos/{}/{}/actions/workflows/{}/runs",
                encode::seg(owner),
                encode::seg(repo),
                encode::seg(workflow_id)
            ))
            .scope("read:repository"),
        );
        self.client.json(req).await
    }
    /// Get a workflow
    ///
    /// `GET /repos/{owner}/{repo}/actions/workflows/{workflow_id}` — needs the `read:repository` token scope.
    pub async fn view(
        &self,
        owner: &str,
        repo: &str,
        workflow_id: &str,
    ) -> Result<gitea_model::ActionWorkflow> {
        let req = Request::get(format!(
            "/repos/{}/{}/actions/workflows/{}",
            encode::seg(owner),
            encode::seg(repo),
            encode::seg(workflow_id)
        ))
        .scope("read:repository");
        self.client.json(req).await
    }
}
